# Point Gradle's toolchain resolver at locally-installed JDKs so the
# `languageVersion = 17` toolchain in build.gradle can be found without
# requiring a system-wide /Library/Java/JavaVirtualMachines symlink.
org.gradle.java.installations.paths=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
