# 백엔드 API 기준 주소 (REQ-NFR-003 — 빌드 시점 환경 설정으로 주입, 소스 하드코딩 금지).
# 로컬 개발: 이 파일을 .env.local로 복사하고 실제 값을 채운다 (.env.local은 gitignore 대상).
# 프론트엔드·백엔드를 동일 오리진에서 서빙하는 배치에서는 빈 문자열로 두면
# 상대 경로 호출이 되어 그대로 동작한다 (design.md §B.2).
VITE_API_BASE_URL=http://localhost:8080
