# SPEC Review Report: SPEC-FRONTEND-001

Iteration: 4/3 (**user-authorized confirming audit beyond the nominal max-3 cap** — the user selected the post-iter-3 escalation option "fix M1-M4, then run a 4th confirming audit", which is the Retry Loop Contract § LEAN Workflow Additions option 3, *explicit user override to continue iterating*)
Verdict: **PASS**
Overall Score: **0.91** (harmonic mean of 4 dimensions) — Tier L PASS threshold is **0.85**

> Reasoning context ignored per M1 Context Isolation. The delegation prompt's factual claims about the v0.2.2 revision (all 4 of M1-M4 applied, REQ 58→59, INV 10→11, AC 85→86, DEP-3 recorded resolved citing `21eab8a`, two-baseline note collapsed to a single `main` baseline with an embedded re-verification command, `--ignore-deps` branch removed from AC-FE-907) were treated as **hypotheses** and re-verified against the artifact bodies, git objects, `gh` API, and Java source on current `main`. All were confirmed true.
>
> **Score progression: iter1 = 0.75 → iter2 = 0.89 → iter3 = 0.83 → iter4 = 0.91. No regression** (+0.08 vs iter3, +0.02 vs the iter2 high-water mark). The round-3 `STOP` signal is **cleared** — its stated cause (an exogenous dependency-state change) has been absorbed into the document set, and the fix has restored the score above both the threshold and the prior peak. No new STOP signal is emitted.

---

## Must-Pass Results

- **[PASS] MP-1 REQ number consistency** — 59 REQ entries mechanically enumerated from `^- \*\*REQ-` lines in `spec.md`. Full ordinal dump, verbatim: SES `001-009` (9), CAT `001-006` (6), ENR `001-011` (11), CNL `001-010` (10, **`REQ-CNL-010` is the new terminal ordinal — no gap**), ADM `001-010` (10), ERR `001-006` (6), NFR `001-007` (7) = **59**. Contiguous `001..N` in every namespace. `sort | uniq -d` over the full ID set returned **zero** duplicates. Consistent 3-digit zero padding throughout. Matches `plan.md:29` ("59개 REQ + 11개 INV"), `progress.md:19` ("요구사항 **59건**"), `acceptance.md:191,193` (§D.1 소계/합계 = 59), and `spec.md:27` (HISTORY: "REQ 총계 58→59").

- **[PASS] MP-2 GEARS format compliance** — Mechanical: `grep -nE '^\- \*\*REQ-[A-Z]+-[0-9]{3}\*\*' spec.md | grep -vE '\((Ubiquitous|Event-driven|State-driven|Capability gate|Where|Unwanted)\)'` returned **zero lines** — all 59 carry an explicit pattern label. A second grep for REQ lines lacking the token `shall` also returned **zero**. Legacy `IF … THEN` count = **0**. The new REQ was read individually: `spec.md:282` — "**REQ-CNL-010** (Ubiquitous) — 클라이언트는 대기 목록의 각 항목에서 `position`을 해당 항목의 `courseTitle`과 나란히 표시 **shall**하며, `position`을 목록 전역의 대기 순위·승격 예정 순서로 읽히는 표현으로 표시 **shall not**한다." This is a well-formed Ubiquitous pattern with a `<subject>` (클라이언트), a positive `shall` clause and a negative `shall not` clause — and it is on the **REQ-bullet surface**, which is exactly what round-3 M2 required (the obligation previously lived only in a `>` rationale blockquote and escaped the MP-2 grep subject entirely).

- **[PASS] MP-3 YAML frontmatter validity** — All 12 canonical fields present with correct types (`spec.md:2-13`), read verbatim: `id: SPEC-FRONTEND-001` (matches `^SPEC-[A-Z][A-Z0-9]+-[0-9]{3}$`), `title` quoted non-empty, `version: "0.2.2"` quoted semver (**correctly bumped** from `"0.2.1"`), `status: draft` (valid enum), `created: 2026-08-17` / `updated: 2026-08-17` ISO dates using **canonical** names (not `created_at`/`updated_at`), `author: manager-spec`, `priority: P1`, `phase: "v1.0.0"`, `module: "frontend"`, `lifecycle: spec-anchored`, `tags:` comma-separated string (not `labels`). Optional `tier: L` (`:14`) and `depends_on:` (`:15`) present and well-formed. Zero rejected snake_case aliases. The v0.2.2 HISTORY row is present at `spec.md:27` and accurately enumerates the four fixes plus all three count deltas.

- **[N/A] MP-4 Section 22 language neutrality** — N/A: single-language-scoped SPEC. Deliverable is a TypeScript/React client under `frontend/` (`REQ-NFR-001`, `spec.md:316`); no multi-language tooling surface. Auto-passes.

- **[PASS] MP-5 D7 cross-SPEC reconciliation** — D7 verb executed. Referenced SPEC IDs extracted from all six artifacts via `grep -ohE 'SPEC-([A-Z][A-Z0-9]+-)+[0-9]+' *.md | sort -u`: `SPEC-AUTH-001`, `SPEC-COURSE-001`, `SPEC-ENROLLMENT-001`, `SPEC-FRONTEND-001` (self). Statuses read from `.moai/specs/<ID>/spec.md` on current `main`: **`SPEC-AUTH-001` = `completed`, `SPEC-COURSE-001` = `completed`, `SPEC-ENROLLMENT-001` = `completed` (v0.3.0)**. None carries `retired` / `superseded` / `archived` → **no D7 BLOCKING finding.**
  - **Reversal of the round-3 note**: in round 3, `SPEC-ENROLLMENT-001` reading `completed` was the discovery that produced critical finding M1, because every audited artifact asserted `draft`/`in-progress`. In this round the D7 reading and the audited SPEC's own assertions **agree** — `spec.md:127,170`, `plan.md:13,77`, `acceptance.md:150`, `research.md:31`, `progress.md:125` all state `status: completed`. The contradiction is gone.

- **[PASS] MP-6 D8 cross-platform discipline** — D8 verb executed: `grep -c 'syscall' *.md` returns `0` for all six artifacts (spec/plan/acceptance/design/research/progress). Per D8-4, absence of `syscall` is auto-PASS.

- **[PASS] MP-7 clarification gate** — `grep -rn '\[NEEDS CLARIFICATION' plan.md research.md` returns **no matches**. (`progress.md:131` mentions the literal token only in a prose statement asserting "0건", and additionally records that the last remaining open decision — the `DEP-3` A/B disposition — was *eliminated by dependency resolution rather than deferred*; `progress.md` is not in the MP-7 subject set regardless.)

**No must-pass criterion failed.**

---

## Category Scores (0.0-1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.90 | 0.75-1.0 band | The round-3 critical defect is fully reversed: `spec.md:121` now opens §A.5 with "**세 건 모두 해소되었다 — 이 SPEC에 남은 외부 차단 요소는 없다**", `spec.md:163-175` is a new `#### DEP-3 — 선행 PR 병합 (해소됨)` sub-section with an observation table, and `plan.md:38` retitles §B to "알려진 차단 요소 (Known Blockers) — **전부 해소됨**". Every M1-listed site was re-read individually and now states the gate is satisfied (`plan.md:13,32,46,72-79,275,306,326,342`; `acceptance.md:150,160,283,291,312`; `research.md:31,317`; `progress.md:125,146`). `plan.md:275` M1's 진입 조건 is now "없음", removing the stall round 3 identified. `plan.md:81-95` correctly **retains** §B.3.1's gate-semantics lesson (which was correct and valuable) while deleting the dead A/B option tables. The M2 obligation is now a numbered requirement rather than blockquote prose. **Deducted for N1** — `research.md`'s two baseline tables (`:9-14`, `:20-29`) still name `sync/SPEC-ENROLLMENT-001` as the "재확인 기준점" even though `spec.md:76` explicitly retired that scheme, and the reconciliation sentence at `research.md:31` mis-enumerates which rows it covers. **Deducted for N3** — `spec.md:284` asserts the REQ-CNL-010 violation is of the type `acceptance.md` §C.1 reserves for the **차단(Blocker)** grade, but `acceptance.md:158` does not list AC-FE-112 among the Blockers. |
| Completeness | 0.88 | 0.75-1.0 band | All required sections present: HISTORY (`spec.md:20-27`, four versions), WHY (`:33`), WHAT (`:70`), REQUIREMENTS (`:204`), INVARIANTS (`:330-342`, 11 rows), ACCEPTANCE CRITERIA (`acceptance.md:23`), and **5 × `### Out of Scope — <topic>` H3 sub-headings** at `spec.md:350,357,366,375,383`, each with concrete `-` bullets (verified specific, e.g. `:355` "취소 대상 조회 API의 **계약 변경** — API 자체는 백엔드가 이미 추가했다"). Tier L 5-artifact set complete and substantive (`design.md` 278 L, `research.md` 330 L — both read in full per the Tier L input contract). M2's fix is complete on **all four** anchor surfaces round 3 demanded: REQ (`spec.md:282`), INV (`spec.md:342` INV-FE-011), AC (`acceptance.md:137` AC-FE-112), and milestone completion criterion (`plan.md:336`) — plus the §D.2 invariant→AC mapping (`acceptance.md:225`). M3's S14 is present in **both** checklists (`acceptance.md:273`, `plan.md:361`) with all four range tokens updated. **Deducted for N2** — `progress.md:74,146` still read "58 REQ / 86 AC", a half-propagated count (AC updated 85→86, REQ left at 58). **Deducted for N1** — the M1 systemic remedy (single `main` baseline + executable re-verification) was applied to `spec.md` but not carried into `research.md`'s parallel baseline tables. |
| Testability | 0.92 | 0.75-1.0 band | 86 ACs, each with a verification-means column ([자동]/[수동]/[검사], `acceptance.md:13-17`). Weasel-word grep (`적절\|합리적\|충분히\|알맞\|적당`) across all six artifacts = **0 matches in every file**. The new AC-FE-112 (`acceptance.md:137`) is genuinely binary-testable — it names the observable condition (`position` rendered alongside that item's `courseTitle`) *and* enumerates the prohibited wordings verbatim ("내 대기 순위"·"승격 예정 순서"), so a human tester does not have to adjudicate what counts as a global-rank label. AC-FE-907 (`:150`) was restated from a pinned claim into an executable observation (`grep '^status:' …` → `status: completed`) with the `--ignore-deps` branch **deleted**, which removes the fabricated-audit-record hazard round 3 identified. `plan.md:95` adds a matching re-verification instruction at run-entry. **Deducted for N3** — AC-FE-112's severity grade is ambiguous between `spec.md:284` (implies Blocker) and `acceptance.md:158-159` (assigns Must). Operationally neutral (see N3), but a tester reading only `spec.md` would mis-grade a failure. |
| Traceability | 0.93 | 0.75-1.0 band | **Mechanically verified both directions.** Forward: a shell loop over all 59 REQ IDs found every one present in `acceptance.md` — **zero uncovered**, including the new `REQ-CNL-010`. Reverse: every `REQ-*` referenced by an AC exists in `spec.md`, with the single exception of `REQ-ADX-002` (`acceptance.md:110`), a *deliberate* cross-SPEC annotation ("백엔드 `REQ-ADX-002` 연동 확인") — same classification as rounds 1-3, not an orphan. Independent unique-ID count `grep -oE '\*\*AC-FE-[0-9]+[a-z]?\*\*' \| sort -u \| wc -l` = **86**, matching §D.1 합계 (`:193`), the self-check expected value (`:208`), and `progress.md:23`. **Row arithmetic re-derived independently from the enumerated ranges, not trusted**: SES `020~035`=16, CAT `040~046`=7, ENR `060,061,062a,062b,062c,063~072,073a,073b`=17, CNL `100~112`=**13** (was 12), ADM `080~093`=14, ERR=7, NFR=7 → **81** subtotal (was 80) + 5 dependency-tracking = **86**. REQ column re-derived: 9+6+11+**10**+10+6+7 = **59**. Both totals match the table. INV→AC mapping is complete at 11/11 (`acceptance.md:215-225`), with the new INV-FE-011 row present. **Deducted for N2** (a stale REQ count in `progress.md`) and **N3** (severity-tier mapping inconsistency). |

Harmonic mean: `4 / (1/0.90 + 1/0.88 + 1/0.92 + 1/0.93) = 4 / 4.4097 = 0.9071` → **0.91**. Tier L threshold 0.85 → **PASS**.

---

## Regression Check (round-3 M1-M4)

Each finding was re-verified against the current document bodies and — for M1, which was factual — against **live git / `gh` / source state**, not against `progress.md`'s self-report table, which was treated as a claim.

| ID | Severity (r3) | Status | Evidence |
|----|---------------|--------|----------|
| **M1** DEP3-RESOLVED-UNRECORDED | **critical** | **RESOLVED** | Ground truth independently re-established, not inherited: `gh pr view 1 --json state,mergedAt,mergeCommit` → `{"state":"MERGED","mergedAt":"2026-08-17T09:04:24Z","mergeCommit":{"oid":"21eab8af2b7d689d04bda98fb94f50110961679c"}}`; `grep '^status:' .moai/specs/SPEC-ENROLLMENT-001/spec.md` → `completed` (v0.3.0); `git ls-tree -r --name-only main -- src/main/java \| grep -i controller` → **five** controllers including `enrollment/EnrollmentController.java` and `waitlist/WaitlistController.java`. Every one of the ~20 sites round 3 enumerated was re-read and corrected. Load-bearing pieces verified individually: (1) `spec.md:127` DEP-3 row now reads "**해소됨** … 병합 커밋 `21eab8a`, 병합 시각 `2026-08-17T09:04:24Z`" mirroring the DEP-1/DEP-2 rows; (2) `plan.md:72-79` §B.3 is now a resolution record with the A/B option tables deleted and §B.3.1's lesson retained; (3) `acceptance.md:150` AC-FE-907 restated as a `status: completed` observation with the sentence "**`--ignore-deps` 우회 분기는 이 AC에 존재하지 않는다**" — the branch is removed, not merely deprioritized, exactly as required; (4) `spec.md:74-76` rewritten to a **single** `main` baseline. See § Staleness-Class Assessment below for the independent test of whether the fix breaks the recurring pattern. |
| **M2** UNVERIFIED-NORMATIVE-OBLIGATION | major | **RESOLVED** | Promoted, not merely reworded. `spec.md:282` **REQ-CNL-010** (Ubiquitous) carries the obligation on the normative REQ-bullet surface with `shall` / `shall not`; `spec.md:342` **INV-FE-011** fixes it as an invariant; `acceptance.md:137` **AC-FE-112** [수동] verifies it and is mapped in §D.2 (`:225`); `plan.md:336` M6 완료 판정 now names it explicitly ("**대기 목록의 각 항목에서 `position`이 `courseTitle`과 함께 표시되고 전역 순위 표기가 사용되지 않음 확인**(`AC-FE-112`, REQ-CNL-010 / INV-FE-011)"). The pre-existing blockquotes at `spec.md:115` and `spec.md:278` were **correctly demoted to rationale** — `:278` now ends "이 사실이 화면 표시에 부과하는 의무는 **`REQ-CNL-010`이 규범으로 고정한다**(이 주석은 그 근거 설명이며, 규범 자체가 아니다)", resolving the normativity ambiguity that was M2's third sub-problem. `plan.md:334` M6 directive 5 likewise redirects to the REQ. Count propagation for this fix verified independently at every surface (see Traceability row). |
| **M3** MANUAL-AC-ABSENT-FROM-DOD-CHECKLIST | minor | **RESOLVED** | `S14 새로고침 후 폴링 재개 → 3초 간격, 상한은 최초 접수 기준 (AC-FE-073b)` added to **both** checklists: `acceptance.md:273` (§E.2, the `[HARD]` human-verification record) and `plan.md:361` (§F M7 시나리오 표). All four range tokens updated to `S1~S14`: `acceptance.md:258`, `acceptance.md:289` (§E.4), `plan.md:256` (§E E9), `plan.md:363` (M7 완료 판정). `grep -rn 'S1~S13\|S1~S9'` across all six artifacts returns **only** the `progress.md:52` change-log row describing the earlier N3 fix — a legitimately historical reference. |
| **M4** AP-NUMBER-ORDER | minor | **RESOLVED** | `grep -oE '^\| AP-[0-9]+' plan.md` now returns, in document order: `AP-1 AP-2 AP-3 AP-4 AP-5 AP-6 AP-7 AP-8 AP-9 AP-10 AP-11 AP-12 AP-13 AP-14` — strictly ascending. Count = 14 with no gaps and no duplicates, so `progress.md:22`'s "안티패턴 **14건**(오름차순 정렬)" is accurate. Spot-verified that the moved rows kept their content: `plan.md:379` AP-7 is still the identifier-direct-input anti-pattern mapping to `REQ-CNL-004`/INV-FE-008, which is the row round 3 cited as the reader-misdirection case. |

**4 of 4 round-3 findings confirmed resolved.** No regression: all 8 round-2 findings (N1-N8) and all 11 round-1 findings (D1-D11) were spot-re-verified as still holding — in particular N1's source claim was re-derived once more from `main` (`WaitlistEntryRepository` course-scoped `nextPosition`), and it is now additionally load-bearing for the new REQ-CNL-010, so a regression there would have surfaced. **No stagnation** — no defect has persisted unchanged across iterations.

---

## Staleness-Class Assessment (the 3-round-recurring defect class)

Round 3 recorded that the class *"the document asserts something untrue about a moving dependency's state"* had appeared in every round (r1 D1/D2/D3 critical, r2 N2 major, r3 M1 critical), and recommended replacing pinned-SHA prose with a **re-verification procedure**. The audit prompt asks specifically whether v0.2.2 genuinely breaks the pattern or merely re-instantiates it with a fresher pinned fact. I tested this directly rather than reading the prose.

**Three independent tests, all passed:**

1. **The pinned SHA is forward-hedged.** `spec.md:74` reads "**단일 기준점이다 — `main` HEAD(커밋 `21eab8a`, 또는 그 이후의 최신 `main` HEAD)**". The parenthetical means the claim does not become false the moment `main` advances — which it already has: `git rev-parse main` → `e7d47cc` (the v0.2.2 docs commit itself), two commits past `21eab8a`. Under round 3's stricter reading a bare pinned SHA would already be stale again; the hedge is what prevents that.

2. **The claim is still true in substance despite `main` having moved.** `git diff --name-only 21eab8a main` returns exactly **9 files, all under `.moai/`** (3 audit reports + the 6 SPEC artifacts) — **zero** changes under `src/` or `build.gradle`. So the endpoint surface the §A.4 table describes is byte-identical at `21eab8a` and at current `main`. The literal SHA has aged; the substance has not.

3. **The embedded re-verification command actually works.** I executed `spec.md:80-88` verbatim rather than assuming it would run. Output: `grep '^status:' …` → `status: completed`; the controller loop emitted mapping annotations for **exactly the 14 endpoints in the §A.4 table**, one-for-one — `/api/auth/signup`, `/api/auth/login`, `GET /api/courses`, `GET /api/courses/{id}`, `POST /api/courses/{courseId}/enrollments`, `GET /api/enrollment-requests/{requestId}`, `DELETE /api/enrollments/{enrollmentId}`, `DELETE /api/waitlist-entries/{entryId}`, `POST /api/admin/courses`, `PATCH /api/admin/courses/{id}`, `POST /api/admin/courses/{id}/close`, `DELETE /api/admin/courses/{id}`, `GET /api/enrollments/mine`, `GET /api/waitlist-entries/mine`. No endpoint in the table is missing from the tree; no endpoint in the tree is missing from the table.

**Verdict on the class: genuinely broken, not re-instantiated.** The artifact now carries a *procedure that survives the dependency moving*, and I confirmed the procedure produces the right answer against a `main` that has in fact already moved. `spec.md:78` states the reasoning explicitly and honestly ("이 표의 사실 진술은 **세 차례 연속으로 낡았다** … 원인은 공통이다 — 움직이는 의존성의 **한 시점 스냅샷**을 prose로 고정했기 때문이다. 따라서 이후 개정자는 SHA를 신뢰하지 말고 **아래 명령을 실제로 실행하여** 이 표를 재확인한다"), and `spec.md:175` + `progress.md:129` record the root cause of the r3 miss without minimizing it. `plan.md:95` and `acceptance.md:150` add the same procedure at the run-entry gate.

**Residual surface (graded as N1, minor, not critical).** The remedy was applied to `spec.md` but **not carried into `research.md`**, whose two baseline tables still nominate the retired `sync/SPEC-ENROLLMENT-001` branch as the "재확인 기준점". This is the one place where the class's original shape survives. It is graded minor rather than major for two verified reasons: (a) the branch still exists and still resolves — `git rev-parse sync/SPEC-ENROLLMENT-001` → `173eed9`, and both local and `origin/` refs are present — so the pointer is stale-but-usable, not broken; and (b) `research.md:31` carries an explicit reconciliation sentence stating those observations are now all confirmable on `main`, which I verified is substantively true (test 2 above). It is not a false claim about the world; it is an un-migrated pointer.

---

## Defects Found (structured defect-list)

All four new findings are **minor**. No critical, no major. None blocks run-phase entry.

**N1. RESEARCH-BASELINE-NOT-MIGRATED — `research.md`:L9-14, L20-29, L31, L101 — The M1 systemic remedy (single `main` baseline + executable re-verification) was applied to `spec.md` but not carried into `research.md`'s parallel baseline tables, and the reconciliation note that covers the gap mis-enumerates its own scope and carries an unverifiable file count. — Severity: minor — Required fix:** three sub-items, all in one file.
  (a) `research.md:12,13,14` (the 0.2.0 재확인 header table) and `:25,27,28` (the §1 방법 table) still name `sync/SPEC-ENROLLMENT-001` in the "재확인 기준점" / "확인 시점 기준" column, as does `:101` ("확인 기준점은 `sync/SPEC-ENROLLMENT-001`(PR #1) HEAD이며"). `spec.md:76` explicitly retired this scheme ("**0.2.1의 두 기준점 방식은 폐기되었다**"), so the two artifacts now prescribe different re-verification baselines for the same observations. Not a false claim — the branch resolves (`git rev-parse sync/SPEC-ENROLLMENT-001` → `173eed9`) and the observations do hold on `main` — but a future reviser following `research.md` is sent to a branch baseline the SPEC has abandoned. Fix: retarget these to `main` (per `spec.md` §A.4) and, where the historical branch reading is worth preserving, mark it as provenance rather than as the re-verification baseline.
  (b) `research.md:31` states the sync-baselined observations are "**§1 표의 하단 3행**, §3, §4, §5". The §1 table's sync-baselined rows are at `:25, :26, :27, :28` — **four** rows, and they are not the bottom three (the bottom row `:29` is baselined "전 범위", not the sync branch). The reconciliation's substance is correct; its enumeration is not. Fix: say "§1 표의 4개 행(전 컨트롤러·전 DTO·JwtTokenProvider·엔드포인트 전수)" or drop the row-position reference.
  (c) `research.md:31` asserts the implementation was "`enrollment`·`waitlist` 패키지, **29개 파일**". Measured under every scope I checked, it is not 29: `git ls-tree -r --name-only 871d247 -- src/main/java/.../enrollment .../waitlist | wc -l` = **28** (identical on `main`); `src/main` any-depth = **28**; `src/test` = **31**; `src/` total = **59**. The sentence is labeled "(0.2.2 갱신)", so it was re-asserted in this revision rather than merely inherited. Fix: correct to 28 (implementation files) or state the counting scope explicitly. *Attribution note: I did not find a scope yielding 29; if one exists (e.g. counting a resource or migration file I did not enumerate), the fix is to name the scope rather than the number.*

**N2. PROGRESS-COUNT-HALF-PROPAGATED — `progress.md`:L74, L146 (+ L119 heading) — The REQ total was not propagated into `progress.md`'s two forward-looking sentences, while the AC total in the same phrase was. — Severity: minor — Required fix:** both sites read "**58 REQ / 86 AC** 전수 기계 검증" — `progress.md:74` ("감사자가 권고한 **델타 범위 재감사**(58 REQ / 86 AC 전수 기계 검증 반복 불필요)") and `:146` ("감사자가 58 REQ / 86 AC 전수 기계 검증의 반복은 불필요하다고 명시"). The current totals are **59 REQ / 86 AC**; round 3's report said "58-REQ / 85-AC". So the phrase is neither a faithful quotation of the auditor (which would be 58/85) nor a statement of current state (59/86) — the AC figure was updated and the REQ figure was not. Notably `progress.md:70` gets this right in the same file ("REQ 58→**59** … AC 85→**86**"), so this is a local miss, not a systemic one. Fix: change both to `59 REQ / 86 AC`. Same-file sub-item: the §E.1.5 heading at `:119` still reads "차단 의존성 상태 (**0.2.0 재확인**)" although its DEP-3 row was rewritten in 0.2.2 and self-labels "(0.2.2에서 정정)" — retitle to 0.2.2.

**N3. AC-112-SEVERITY-GRADE-INCONSISTENT — `spec.md`:L284; `acceptance.md`:L158-159 — The M2 fix's rationale invokes the Blocker-grade definition for the new obligation, but the new AC is not listed among the Blockers. — Severity: minor — Required fix:** `spec.md:284` states the REQ-CNL-010 violation makes it so that "**화면이 사실이 아닌 것을 사용자에게 말하게 된다**" and continues: "이는 `acceptance.md` §C.1의 **차단 등급 정의**("사용자에게 사실과 다른 정보를 전달")에 해당하는 결함 유형이며". But `acceptance.md:158`'s 차단(Blocker) row enumerates AC-FE-001·002, 900·903·904, 061·102, 063·064, 030, 109, 108 — **AC-FE-112 is absent**, so it falls into 필수(Must) via `:159` ("§B.1~§B.7의 나머지 전부"). The precedent cuts against this: **AC-FE-030** is graded Blocker for exactly the analogous reason (INV-FE-004 — the screen implying a security property the backend does not have), and `acceptance.md:169-170` grades AC-FE-109 and AC-FE-108 Blocker on the same "screen misinforms the user" logic. Graded **minor rather than major** because the practical effect is nil: `acceptance.md:289` §E.4 admits **no partial-completion path** ("§B.1~§B.7 전부와 §E.2의 S1~S14 전부를 충족했을 때만 완료로 선언한다"), so Blocker and Must produce the identical terminal gate for this SPEC. It is a documentation contradiction, not a coverage hole. Fix: either add AC-FE-112 to the 차단 row at `acceptance.md:158` with a one-line rationale in §C.1 (consistent with the AC-FE-108/109 precedent), or soften `spec.md:284` to reference the *defect character* without invoking the grade name.

**N4. REVERIFY-CMD-JAVADOC-FALSE-POSITIVE — `spec.md`:L85-87 — The embedded re-verification command emits one non-endpoint line, so its raw output line count (15) does not equal the table's row count (14). — Severity: minor — Required fix:** I executed the command verbatim; it works and correctly surfaces all 14 endpoints (see § Staleness-Class Assessment test 3). However `git show "main:…/EnrollmentController.java" | grep -nE '@(Get|Post|Put|Patch|Delete)Mapping|@RequestMapping'` also matches line 25, a **javadoc sentence** containing `{@code @RequestMapping}`: `25: * 수준 {@code @RequestMapping}을 두지 않고 메서드마다 전체 경로를 명시한다.` A reviser who compares output line count against the "14행" the surrounding prose names will see 15 and have to diagnose the discrepancy. This does not produce a wrong conclusion (the line is self-evidently a comment) — hence minor, and I record explicitly that the mitigation still functions. Fix: add a comment filter to the grep, e.g. append `| grep -v '^\s*[0-9]*:\s*\*'`, or note in the prose that javadoc mentions may appear.

---

## Chain-of-Verification Pass

Second-look findings — what the first pass under-executed, and how the second pass corrected it:

- **Did I re-derive M1's ground truth myself, or accept "the SPEC now says resolved"?** Re-derived, and this was the instruction most at risk of being under-executed on a confirming audit. I ran `gh pr view 1` (MERGED / `21eab8a` / `2026-08-17T09:04:24Z`), read `SPEC-ENROLLMENT-001`'s frontmatter on `main` (`completed`), and listed `main`'s controller set (five, including both new ones). I did **not** treat `progress.md`'s M1 row as evidence.
- **Did I test whether the *mitigation* works, or only whether the pinned SHA is current?** The prompt warned that a current SHA is the wrong test. I executed the embedded command verbatim and matched its output one-for-one against the 14-row §A.4 table, and separately ran `git diff --name-only 21eab8a main` to establish that `main` has advanced by two commits with **zero** `src/` delta — so the SHA has aged while the substance has not. Had I checked only "is `21eab8a` still HEAD?" I would have produced a **false FAIL**, because it is not HEAD. This is the check the first pass nearly skipped.
- **Did I actually read every REQ, or accept the count?** All 59 read individually. The second pass asked the narrower "*what did the M2 fix add, and is what it added itself well-formed?*" — the same question that surfaced M2 in round 3 — and confirmed REQ-CNL-010 is on the normative bullet surface, is GEARS-labeled, and that the two surviving blockquotes were correctly demoted with an explicit "규범 자체가 아니다" disclaimer. That same question is what surfaced **N3**: the fix is structurally complete but its severity tier contradicts the rationale that motivated it.
- **Did I check REQ sequencing end-to-end, or spot-check?** Mechanically, per namespace, with a full ordinal dump and a duplicate check, output recorded verbatim under MP-1. The new terminal ordinal `REQ-CNL-010` was specifically checked for a gap at 009→010.
- **Did I verify traceability for every REQ, or sample?** Every REQ, both directions, via shell loops over the full 59-ID set and the full AC→REQ reference set. The reverse loop surfaced only `REQ-ADX-002`, re-read in context and re-classified as the same deliberate cross-SPEC annotation identified in rounds 1-3.
- **Did I re-derive the §D.1 arithmetic, or read the totals?** Re-derived from the enumerated ID ranges independently (16+7+17+**13**+14+7+7 = 81; +5 = 86) and the REQ column (9+6+11+**10**+10+6+7 = 59), then cross-checked against an independent unique-ID grep (86) and against the per-namespace REQ dump (59). Both totals hold. This is what exposed **N2** — the totals are right everywhere the table lives, and wrong only in two prose sentences in `progress.md`.
- **Did I check Out of Scope for specificity, not just presence?** Yes — five H3 sub-headings at `spec.md:350,357,366,375,383`, each re-read for concrete `-` bullets naming specific excluded artifacts (e.g. `:372` "강좌 검색·필터·정렬 — `GET /api/courses`가 page·size만 받는다"). All substantive; unchanged and still valid in v0.2.2.
- **Did I read design.md and research.md, per the Tier L input contract?** Both in full. `design.md` is unchanged at 0.2.1 and `progress.md:21` claims "3회차 지적 사항 해당 없음 — 변경 없음"; I verified this claim rather than accepting it — `design.md:113` correctly carries `AC-FE-073a`/`AC-FE-073b` (the N5 split), `design.md:31-33` covers the `waitlistEntryId`/`position` type-level distinction, and none of M1-M4's edit sites falls in `design.md`'s structural scope. The claim holds. `research.md` is where **N1** came from — it would have been invisible to an audit that read only the primary trio.
- **Did I look for contradictions *between* documents, not just within?** Yes. N1 is a `spec.md`-vs-`research.md` disagreement about the re-verification baseline; N3 is a `spec.md`-vs-`acceptance.md` disagreement about severity grade. Neither is visible reading any single artifact in isolation — which is the same failure mode that produced round 3's M1.
- **Adjustments made:** Clarity was 0.92 in my draft; downgraded to 0.90 after N3 was identified, because a severity contradiction between the requirement's rationale and the acceptance matrix is a clarity defect even when operationally neutral. Completeness was 0.90; downgraded to 0.88 once N1(a) was established as an *un-migrated remedy* rather than a mere wording nit — the systemic fix round 3 recommended was applied to one artifact and not its sibling. I considered grading N1 as **major** on the grounds that the recurring class lives there, and rejected that: I verified the branch still resolves and that the observations genuinely hold on `main`, so unlike r1/r2/r3 there is **no false statement about the world** — grading it major would be punishing the class rather than the defect.

---

## Retry Loop Contract — Status

```
iter1 = 0.75 (FAIL)  →  iter2 = 0.89 (PASS)  →  iter3 = 0.83 (FAIL, STOP)  →  iter4 = 0.91 (PASS)
                                                                                ^ +0.08, no regression
```

- **Score regression check: NOT triggered.** iter4 (0.91) > iter3 (0.83), and also exceeds the iter2 peak (0.89). No new `STOP` signal.
- **Round-3 STOP signal: cleared.** Its stated cause was exogenous (a dependency merged between audits), and round 3's own diagnosis explicitly declined to recommend scope reduction. The bounded mechanical edit pass it recommended instead has been executed and verified.
- **Iteration cap: this is iteration 4, one past the nominal max of 3.** It was run under the user's explicit override (Retry Loop Contract § LEAN Workflow Additions, post-iter-3 option 3), which the contract admits as a conscious choice rather than silent drift. This audit closes that override: the verdict is PASS, so no further iteration is indicated and none is requested.
- **Stagnation: none.** No defect has persisted unchanged across any two iterations. Across four rounds the SPEC has resolved 11 + 8 + 4 = **23 findings**, and this round found **no unresolved carry-over**.

---

## Recommendation

**Verdict is PASS at 0.91** (Tier L threshold 0.85). All seven must-pass criteria pass or are N/A. All four round-3 findings — including the critical M1 — are resolved and were re-verified against live git / `gh` / source state rather than against the SPEC's own self-report. Four new findings were identified, **all minor**, none blocking.

The v0.2.2 pass is the strongest of the four revisions, for a reason worth recording: it is the first one that fixed the *class* rather than the *instance*. Rounds 1-3 each corrected a point-in-time factual claim about `SPEC-ENROLLMENT-001`, and each correction went false again before the next audit. v0.2.2 replaced the snapshot with an executable procedure plus a forward-compatible SHA hedge — and I confirmed that this actually works by running the procedure against a `main` that has already advanced past the pinned SHA. Under round 3's methodology that advance would have produced a fourth instance of the class; under v0.2.2's it does not.

**Disposition: proceed.** The four minor findings do **not** warrant a fifth audit iteration and do not gate run-phase entry. Recommended handling — a single un-audited cleanup edit folded into the next commit that touches these files (for example, the run-phase's first `progress.md` update):

1. **N1 (minor)** — retarget `research.md`'s baseline columns (`:12,13,14,25,27,28,101`) from `sync/SPEC-ENROLLMENT-001` to `main`, matching `spec.md` §A.4; correct the "하단 3행" enumeration at `:31`; correct or scope the "29개 파일" count at `:31` (measured 28 for implementation files).
2. **N2 (minor)** — `progress.md:74,146` `58 REQ` → `59 REQ`; retitle the §E.1.5 heading at `:119` from "(0.2.0 재확인)" to "(0.2.2 재확인)".
3. **N3 (minor)** — either add AC-FE-112 to the 차단 row at `acceptance.md:158` (consistent with the AC-FE-108/109 precedent), or soften the grade-name invocation at `spec.md:284`. Pick one; do not leave both as-is.
4. **N4 (minor)** — add a comment filter to the `spec.md:85-87` re-verification grep so its output line count matches the table's 14 rows.

**Run-phase entry is not externally blocked, and the SPEC now says so correctly.** `SPEC-AUTH-001`, `SPEC-COURSE-001`, and `SPEC-ENROLLMENT-001` are all `status: completed` on `main`; PR #1 is `MERGED` (`21eab8a`); the `depends_on` pre-flight passes on all three dependencies; `AC-FE-907`'s check command returns `status: completed` when executed. **Implementation Kickoff Approval (the plan→run HUMAN GATE) remains mandatory and is unaffected by this PASS verdict** — a plan-auditor PASS is never a substitute for it, and skip-eligibility governs only Phase 1 verdict *re-execution*. On that separate question: this verdict satisfies conditions 1 (`PASS`) and 2 (score 0.91 ≥ 0.90) of the 4-condition skip contract, but the orchestrator MUST still verify conditions 3 (plan-artifact hash unchanged since this verdict — note that applying the four N-fixes above would touch `acceptance.md`, invalidating it) and 4 (within 24h) at run time before taking the skip.
