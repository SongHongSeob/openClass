# SPEC Review Report: SPEC-FRONTEND-001

Iteration: 3/3 (final — invoked as the `/moai run` Phase 1 Plan Audit Gate)
Verdict: **FAIL**
Overall Score: **0.83** (harmonic mean of 4 dimensions) — Tier L PASS threshold is **0.85**

> Reasoning context ignored per M1 Context Isolation. The delegation prompt's factual claims about the v0.2.1 revision (8 fixes applied, AC total 84→85, spec.md bumped to v0.2.1) were treated as hypotheses and re-verified against the artifact bodies, git objects, and Java source. All were confirmed true. **However, a new critical defect was found that is larger than any of N1-N8: the SPEC's single stated run-phase entry blocker (`DEP-3`) was resolved before the v0.2.1 commit landed, and no artifact records it.**
>
> **Score regression check: iter1 = 0.75 → iter2 = 0.89 → iter3 = 0.83. Score REGRESSED (−0.06).** Per the Retry Loop Contract § LEAN Workflow Additions, a **`STOP` signal is emitted** — see § STOP Escalation below. The orchestrator MUST NOT iterate further unconditionally.

---

## Must-Pass Results

- **[PASS] MP-1 REQ number consistency** — 58 REQ entries mechanically enumerated from `^- \*\*REQ-` lines in `spec.md`: SES `001-009` (9), CAT `001-006` (6), ENR `001-011` (11), CNL `001-009` (9), ADM `001-010` (10), ERR `001-006` (6), NFR `001-007` (7) = **58**. Full per-namespace ordinal dump shows contiguous `001..N` in every namespace — no gaps. `sort | uniq -d` on the full ID set returned zero duplicates. Consistent 3-digit zero padding throughout. Matches `plan.md:29` ("58개 REQ + 10개 INV") and `progress.md:19` ("요구사항 **58건**"). Unchanged from v0.2.0; the v0.2.1 pass added no REQs.

- **[PASS] MP-2 GEARS format compliance** — Mechanical: `grep -nE '^\- \*\*REQ-[A-Z]+-[0-9]{3}\*\*' spec.md | grep -vE '\((Ubiquitous|Event-driven|State-driven|Capability gate|Where|Unwanted)\)'` returned **zero lines** — all 58 carry an explicit pattern label. Label distribution: Ubiquitous 30, Event-driven 23, State-driven 4, Capability gate 1. A second grep for REQ lines lacking the token `shall` also returned **zero**. Legacy `IF … THEN` count = **0**. Spot-verified the two REQs the v0.2.1 pass touched in their rationale blocks: `spec.md:249` REQ-CNL-008 body is unchanged and correct ("…표시 **shall**하며, 자체 기준으로 재정렬 **shall not**한다"); `spec.md:213` REQ-ENR-004's complement rule intact.
  - Caveat folded into M2 below: the v0.2.1 N1 fix introduced `shall`/`shall not` tokens **inside a rationale blockquote** (`spec.md:253`) rather than in a REQ bullet. This does not fail MP-2 (the grep subject is the REQ-bullet surface, and all 58 bullets pass) but it is a real defect — see Defects Found M2.

- **[PASS] MP-3 YAML frontmatter validity** — All 12 canonical fields present with correct types (`spec.md:2-13`), read verbatim: `id: SPEC-FRONTEND-001` (matches `^SPEC-[A-Z][A-Z0-9]+-[0-9]{3}$`), `title` quoted non-empty, `version: "0.2.1"` quoted semver (**correctly bumped** from `"0.2.0"`), `status: draft` (valid enum), `created: 2026-08-17` / `updated: 2026-08-17` ISO dates using **canonical** names (not `created_at`/`updated_at`), `author: manager-spec`, `priority: P1`, `phase: "v1.0.0"`, `module: "frontend"`, `lifecycle: spec-anchored`, `tags:` comma-separated string (not `labels`). Optional `tier: L` (`spec.md:14`) and `depends_on:` (`spec.md:15`) present and well-formed. Zero rejected snake_case aliases. The v0.2.1 HISTORY row is present at `spec.md:26`.

- **[N/A] MP-4 Section 22 language neutrality** — N/A: single-language-scoped SPEC. Deliverable is a TypeScript/React client under `frontend/` (`REQ-NFR-001`, `spec.md:288`); no multi-language tooling surface. Auto-passes.

- **[PASS] MP-5 D7 cross-SPEC reconciliation** — D7 verb executed. Referenced SPEC IDs extracted from all six artifacts: `SPEC-AUTH-001`, `SPEC-COURSE-001`, `SPEC-ENROLLMENT-001`, `SPEC-FRONTEND-001` (self). Statuses read from `.moai/specs/<ID>/spec.md` on current `main`: **all three dependencies are `status: completed`.** None carries `retired` / `superseded` / `archived` → **no D7 BLOCKING finding.**
  - **Note (feeds M1, not MP-5):** `SPEC-ENROLLMENT-001` reading `completed` is itself the discovery that produced critical finding M1 — the SPEC-FRONTEND-001 artifacts assert it is `draft`/`in-progress`. That is a currency defect in the audited SPEC, **not** a D7 retirement conflict, so it does not fail MP-5. It is graded at severity=critical in Defects Found.

- **[PASS] MP-6 D8 cross-platform discipline** — D8 verb executed: `grep -c 'syscall' *.md` returns `0` for all six artifacts (spec/plan/acceptance/design/research/progress). Per D8-4, absence of `syscall` is auto-PASS.

- **[PASS] MP-7 clarification gate** — `grep -rn '\[NEEDS CLARIFICATION' plan.md research.md` returns **no matches**. (`progress.md:114` mentions the literal token only in a prose statement asserting "0건"; `progress.md` is not in the MP-7 subject set.)

**No must-pass criterion failed.** The FAIL verdict is driven by the aggregate score falling below the Tier L threshold **together with an open critical finding (M1)** — not by the M5 firewall.

---

## Category Scores (0.0-1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.80 | 0.75-1.0 band | Genuine improvement from the v0.2.1 pass: the false "승격 예정 순서" semantic claim is gone and replaced with a source-grounded correction (`spec.md:104`, `:253`; `plan.md:351`), and both stale cross-references are fixed (`research.md:31` → `§B.3`, `:211` → `§F M1`). **Deducted for M1** — roughly 20 load-bearing statements across all six artifacts assert that run-phase entry is blocked by `DEP-3`, which is false; an implementer reading `plan.md:292` ("진입 조건: `DEP-3` 처리 방침 확정") cannot correctly determine whether they may start. **Deducted for M2** — `spec.md:253` places `shall`/`shall not` inside a rationale blockquote, leaving the obligation's normative status ambiguous (§B is titled "요구사항 (GEARS)" and REQ bullets are the normative surface everywhere else). |
| Completeness | 0.75 | 0.75 band | All required sections present: HISTORY (`spec.md:20-26`, three versions), WHY (`:32`), WHAT (`:69`), REQUIREMENTS (`:179`), ACCEPTANCE CRITERIA (`acceptance.md:23`), and **5 × `### Out of Scope — <topic>` H3 sub-headings** at `spec.md:321,328,337,346,354`, each with concrete `-` bullets (e.g. `:326` "취소 대상 조회 API의 **계약 변경** — API 자체는 백엔드가 이미 추가했다"). Tier L 5-artifact set complete and substantive (design.md 277 L, research.md 329 L). Round-2 findings N2 and N8 both repaired (`spec.md:73` two-baseline provenance with real SHAs; `research.md:153-157` invalidation note now symmetric with §2.3). **Deducted for M1** — this is the same defect class round 2 scored N2 under (a false factual claim about backend state), but an order of magnitude larger: ~20 sites vs 1 line, and load-bearing (the SPEC's sole entry gate) vs a provenance footnote. **Deducted for M2** — the new obligation has no INV entry (INV list is still exactly 10, `spec.md:304-313`) and no AC anchor. |
| Testability | 0.87 | 0.75-1.0 band | 85 ACs, each with a verification-means column ([자동]/[수동]/[검사], `acceptance.md:13-17`). Weasel-word grep (`적절|합리적|충분히|알맞|적당`) across all six artifacts = **0 matches in every file**. The N5 fix is a real improvement: `acceptance.md:95` AC-FE-073a is now genuinely unit-testable ("접수 시각이 현재로부터 20초 전으로 보존되어 있는 … 스케줄을 새로 계산하면 … **3초** … 보존된 접수 시각으로부터 30초") and `:96` AC-FE-073b carries the browser observation as [수동]. **Deducted for M2** — a stated obligation the SPEC itself calls INV-FE-004-family has no AC at all (`grep 'courseTitle' acceptance.md` returns only AC-FE-902, which checks response field names, not screen rendering). **Deducted for M3** — the new [수동] AC-FE-073b is absent from the §E.2 human checklist. |
| Traceability | 0.90 | 0.75-1.0 band | **Mechanically verified both directions.** Forward: a shell loop over all 58 REQ IDs found every one present in `acceptance.md` — **zero uncovered**. Reverse: every `REQ-*` referenced by an AC exists in `spec.md`, with the single exception of `REQ-ADX-002` (`acceptance.md:110`), which is a *deliberate* cross-SPEC annotation ("백엔드 `REQ-ADX-002` 연동 확인"), not an orphan — same classification as rounds 1-2. The N5 count propagation is fully consistent: independent `grep -oE '\*\*AC-FE-[0-9]+[a-z]?\*\*' \| sort -u \| wc -l` = **85**, matching §D.1 합계 (`acceptance.md:192`), the self-check expected value (`:207`), and `progress.md:23`. Row arithmetic re-derived independently: 16+7+**17**+12+14+7+7 = **80** subtotal + 5 dependency-tracking = **85**. ENR row correctly enumerates `073a, 073b` (`:185`). N4 repaired: column renamed to "구간(마일스톤)" (`:181`) with an explicit note (`:194`) that cross-group REQ references are expected. **Deducted only for M2** (a normative obligation living outside the REQ namespace with no traceability anchor). |

Harmonic mean: `4 / (1/0.80 + 1/0.75 + 1/0.87 + 1/0.90) = 4 / 4.8439 = 0.8258` → **0.83**. Tier L threshold 0.85 → **FAIL**.

---

## Regression Check (round-2 N1-N8)

Each finding was re-verified against the current document bodies and, where the finding was factual, against **source** — not against the `progress.md:48-57` self-report table, which was treated as a claim.

| ID | Severity (r2) | Status | Evidence |
|----|---------------|--------|----------|
| **N1** WAITLIST-ORDER-SEMANTICS-FALSE | major | **RESOLVED** | Underlying fact independently re-derived from Java source on `main`: `WaitlistEntryRepository` line 23 declares `@Query("SELECT COALESCE(MAX(w.position), 0) + 1 FROM WaitlistEntry w WHERE w.courseId = :courseId") long nextPosition(...)` — **course-scoped**, confirmed verbatim. `WaitlistEntry.java:19` javadoc names the partial unique index `(course_id, position) WHERE status='WAITING'`. `WaitlistEntryRepository:56` `findByMemberIdAndStatusOrderByPositionAsc(Long memberId, WaitlistStatus status)` therefore sorts a cross-course set by a per-course rank. The v0.2.1 correction states exactly this. `grep '승격 예정 순서'` now returns **only negations**: `spec.md:253` "그것은 **승격 예정 순서가 아니다**", `plan.md:351` identical. The REQ-CNL-008 rationale (`spec.md:251`) was rewritten to the true reason ("백엔드 순서가 결정적이며 … 재정렬하면 … AC-FE-110 … 검증하려는 대상 자체가 사라진다"). `spec.md:103` ordering note softened as required ("이 정렬은 결정적 표시 순서일 뿐 강좌를 가로지르는 어떤 의미도 갖지 않는다"), and a new §A.4 bullet at `:104` states the course-scoped fact with the backend evidence. `plan.md:350` (M6 item 4) no longer carries the claim; new item 5 at `:351` carries the prohibition. Fix went beyond the requirement. |
| **N2** STALENESS-BASELINE-CLAIM-FALSE | major | **RESOLVED** | `spec.md:73` now reads "**1~4·9~12번은 `main` HEAD(커밋 `29a1560`)**, **5~8·13~14번은 `sync/SPEC-ENROLLMENT-001` HEAD(PR #1, 커밋 `871d247`)**". Both SHAs verified to exist and to contain what is claimed: `git ls-tree -r --name-only 29a1560 -- src/main/java \| grep -i controller` returns exactly `CourseController`, `CourseAdminController`, `AuthController` — which supply endpoints 1-4 and 9-12, and **only** those, so the split is correct. `git show 871d247:.../EnrollmentController.java` contains `@GetMapping("/api/enrollments/mine")` (line 107) and `@GetMapping("/api/enrollment-requests/{requestId}")` (line 76); `git show 871d247:.../WaitlistController.java` contains `@GetMapping("/api/waitlist-entries/mine")` (line 45) — so 5-8 and 13-14 are verifiable there. Timeline check: the next commits on the sync branch (`c80087e` 18:02:50, `09e79d0` 18:03:00, `173eed9` 18:03:56) all postdate the v0.2.1 edit (spec.md mtime 17:40), so **`871d247` genuinely was the sync-branch HEAD when the claim was written.** The rationale sentence at `:75` and the `research.md:31` agreement were both added. N2 is resolved as specified. *(That both "HEAD" labels have since gone stale is a consequence of M1, graded there — not a failure of the N2 fix.)* |
| **N3** SCENARIO-RANGE-STALE | minor | **RESOLVED** | `acceptance.md:256` now reads "`plan.md` §F M7의 시나리오 **S1~S13**을 사람이 브라우저에서 수행하고 **결과를 기록**한다", matching the 13-entry checklist at `:258-270`, `plan.md:379` M7 완료 판정 ("S1~S13 전부 수행"), and `plan.md:273` E9. `grep -rn 'S1~S9'` across all artifacts returns **only** the `progress.md:52` change-log row describing the fix. |
| **N4** TRACEABILITY-ROW-ATTRIBUTION | minor | **RESOLVED** | §D.1's header column is now "**구간(마일스톤)**" (`acceptance.md:181`), and a new explanatory note at `:194` states the rows group by acceptance section (= `plan.md` §F milestone), not REQ namespace, names the exact examples round 2 cited (AC-FE-103 → `REQ-ENR-004` in the CNL row; AC-FE-021·022·024·046 referencing ERR-group REQs from SES/CAT rows), and scopes what the table guarantees to exactly two claims (one-row membership; every REQ covered). I spot-verified all four cited examples against `acceptance.md:128, 48, 49, 51, 74` — every attribution in the note is accurate. |
| **N5** AC-073-MEANS-MISMATCH | minor | **RESOLVED** | Split executed as recommended: `acceptance.md:95` **AC-FE-073a** [자동] (remount-equivalent recomputation against a persisted 20-s-old receipt timestamp) and `:96` **AC-FE-073b** [수동] (browser refresh reproduces it). Propagation verified at every site round 2 named: §D.1 ENR row 16→**17** and enumeration includes `073a, 073b` (`:185`); 소계 79→**80** (`:190`); 합계 84→**85** (`:192`); self-check expected value **85** (`:207`); counting rule now lists `073a`·`073b` among the independent sub-IDs (`:198`); `progress.md:23` = 85. Downstream references updated: `plan.md:168`, `plan.md:324` (M4 완료 판정), `design.md:113` all read `AC-FE-073a … AC-FE-073b`. **Zero bare `AC-FE-073` references remain outside HISTORY/change-log prose** (`spec.md:26`, `progress.md:42`, `progress.md:54` — all legitimately historical). My independent unique-ID count returns exactly **85**, and a duplicate-definition check on table column 1 returned empty. |
| **N6** XREF-M1-M2 | minor | **RESOLVED** | `research.md:211` now reads "`plan.md` §F **M1**이 이를 확인 작업으로 배치한다(`acceptance.md` §F F4도 "M1 수행 중"으로 기록한다)". Target verified: `plan.md:297` places the `@Valid` body-shape observation in M1; `acceptance.md:305` F4 says "M1 수행 중". Consistent. |
| **N7** XREF-C1-B3 | minor | **RESOLVED** | `research.md:31` now reads "`plan.md` **§B.3**이 이를 선행 조건(`DEP-3` 진입 게이트)으로 다룬다". Verified: `plan.md:72` is §B.3 (DEP-3). |
| **N8** RESEARCH-3.2-UNMARKED | minor | **RESOLVED** | `research.md:153` heading now reads "### 3.2 판정 (0.1.0 조사 시점 — **§3.0에 의해 무효화됨**)", and `:157` carries the inline note "> **이 판정은 현재 유효하지 않다** (§3.0). … 위 문단은 백엔드 변경의 근거가 된 조사 기록으로만 남긴다." This is now byte-symmetric in form with the §2.3 note at `:85`. Asymmetry eliminated. |

**8 of 8 round-2 findings confirmed resolved.** No literal stagnation — no defect persisted unchanged across iterations, and manager-spec made real, source-grounded progress in both revision passes.

**Class-level recurrence (distinct from stagnation):** the defect class *"the document asserts something untrue about the backend's current state"* has now appeared in **all three rounds** — round 1 D1/D2/D3 (all critical), round 2 N2 (major), round 3 M1 (critical). Each individual instance was fixed; the class keeps regenerating because the SPEC pins point-in-time facts about a moving dependency without a mechanism that survives the dependency moving. This is a structural observation, not a stagnation flag.

---

## Defects Found (structured defect-list)

**M1. DEP3-RESOLVED-UNRECORDED — `spec.md`:L116, L73, L75, L230; `plan.md`:L13, L40, L46, L70, L72-112, L292, L323, L343; `acceptance.md`:L159, L288, L309; `research.md`:L31, L317; `progress.md`:L110, L127 — The SPEC's single stated run-phase entry blocker (`DEP-3`, "PR #1 미병합") was resolved *before* the v0.2.1 commit landed, and no artifact records it. — Severity: critical — Required fix:** every artifact asserts `DEP-3` is unresolved and that the `depends_on` pre-flight blocks the entire `/moai run` invocation (`spec.md:116` "**미해소 — 유일하게 남은 차단 요소**"; `plan.md:88` "**게이트는 마일스톤 단위가 아니라 `/moai run` 호출 단위로 막는다. M1도 시작할 수 없다.**"; `plan.md:13` "`SPEC-ENROLLMENT-001`(**`in-progress`·PR #1 미병합**)"; `research.md:31` "`main`에 **아직 병합되지 않았다**"; `progress.md:110` "**미해소 — 유일한 차단 요소**"). Source verification contradicts all of it:

- `gh pr view 1 --json state,mergedAt,mergeCommit` → `{"state":"MERGED","mergedAt":"2026-08-17T09:04:24Z","mergeCommit":{"oid":"21eab8af2b7d689d04bda98fb94f50110961679c"}}`
- `git log -1 --format='%h %ci %s' 21eab8a` → `21eab8a 2026-08-17 18:04:24 +0900 Merge pull request #1 from SongHongSeob/sync/SPEC-ENROLLMENT-001`
- `grep '^status:' .moai/specs/SPEC-ENROLLMENT-001/spec.md` on `main` → `status: completed`
- `git ls-tree -r --name-only main -- src/main/java | grep -i controller` → **five** controllers, now including `enrollment/EnrollmentController.java` and `waitlist/WaitlistController.java`; `grep -cE 'enrollment|waitlist'` over `main`'s java tree → **28 files**

The v0.2.1 docs commit `3d84a23` landed at **18:07:07** — **2 m 43 s after the merge commit** — so the assertion was already false at commit time; this is not merely "the world moved after publication". Consequences, in order of harm: (a) `AC-FE-907` (`acceptance.md:149`) and `plan.md:107` instruct that the `--ignore-deps` branch must record "미충족 의존성 ID와 우회 사유" in `.moai/logs/depends-on-override.log` — exercising that branch now would **fabricate an audit record asserting an unfulfilled dependency that is in fact fulfilled**, a direct violation of `verification-claim-integrity.md` §1.1 surface 3; (b) `plan.md:292` makes M1's entry condition a decision ("§B.3 선택지 A 또는 B") that no longer exists, so an implementer following plan.md literally stalls at M1 awaiting a moot orchestrator ruling; (c) `plan.md:94-112`'s entire two-option cost analysis is dead weight that a reader must diagnose as stale before proceeding.

Sub-defect (same root cause): `spec.md:73`'s two-baseline provenance note — the very mechanism round 2 required so the *next* reviser could re-verify mechanically — is now itself misleading. Both "HEAD" labels are stale (`git rev-parse main` → `3d84a23…`, not `29a1560`; `git rev-parse sync/SPEC-ENROLLMENT-001` → `173eed9…`, not `871d247`), the two-baseline split is no longer necessary (all 14 endpoints are verifiable on `main` at `21eab8a`), and `spec.md:75`'s present-tense rationale "`main` HEAD에 존재하는 컨트롤러는 … **3개뿐**" is false against current `main` (five). This is the second consecutive round in which this one note has been found false.

Fix: (1) mark `DEP-3` **해소** in `spec.md:116` with the merge commit `21eab8a` + merged-at timestamp as evidence, mirroring the `DEP-1`/`DEP-2` rows; (2) collapse `plan.md` §B.3 (`:72-112`) from a two-option decision to a resolution record — retain §B.3.1's gate-semantics lesson (it is correct and valuable) but delete the A/B option tables; (3) update `plan.md:13`, `:40`, `:46`, `:70`, and the M1/M4/M6 entry conditions (`:292`, `:323`, `:343`) to state the gate is satisfied; (4) update `acceptance.md:159`, `:288`, `:309` and restate `AC-FE-907` so its PASS condition is `status: completed` observed at run-phase entry, explicitly **removing** the `--ignore-deps` branch (it is no longer reachable and must not be invoked); (5) update `research.md:31` and `:317` (gap #3 is closed); (6) update `progress.md:110`, `:127`; (7) rewrite `spec.md:73-75` to a **single** baseline — `main` at `21eab8a` — verifiable for all 14 endpoints, and record the merge as the reason the two-baseline scheme was retired.

**M2. UNVERIFIED-NORMATIVE-OBLIGATION — `spec.md`:L253, L104; `plan.md`:L351 — The N1 fix introduced a new `shall`/`shall not` obligation that has no acceptance criterion, no invariant, and no milestone completion item, and that lives in a rationale blockquote rather than a REQ bullet. — Severity: major — Required fix:** `spec.md:253` states "화면은 `position`을 반드시 `courseTitle`과 나란히 렌더링 **shall**하며, 목록 전역의 대기 순위처럼 보이는 표현을 사용 **shall not**한다. 이를 어기면 **INV-FE-004 계열의 결함**(화면이 사실이 아닌 것을 사용자에게 말함)이 된다." `spec.md:104` carries the same obligation in the §A.4 contract notes ("반드시 `courseTitle`과 나란히 표시하고 … 표시해서는 **안 된다**"), and `plan.md:351` restates it as M6 implementation directive 5. Three problems: (i) **no verification** — `grep 'courseTitle' acceptance.md` returns exactly one hit, `AC-FE-902` (`:144`), which checks *response field names and client type declarations*, not screen rendering; nothing tests adjacency or the global-rank prohibition, and `plan.md:353` M6 완료 판정 does not mention it; (ii) **no invariant** — the SPEC classifies the violation as INV-FE-004-family, but the INV table is still exactly 10 rows (`spec.md:304-313`, `grep -cE '^\| INV-FE-'` = 10) and INV-FE-004's only verification AC is `AC-FE-030` (logout wording, `acceptance.md:217`); (iii) **ambiguous normativity** — every other `shall` in this SPEC sits in a `^- **REQ-…**` bullet under §B "요구사항 (GEARS)"; placing one in a `>` rationale block leaves an implementer unable to tell whether it binds, and it escapes the MP-2 grep surface entirely. This matters because the SPEC's own §C.1 (`acceptance.md:161-169`) reserves Blocker grade for exactly this failure mode ("사용자에게 사실과 다른 정보를 전달"), and N1's whole point was that an implementer would otherwise mislabel the list. Fix: promote the obligation to a numbered requirement (e.g. `REQ-CNL-010`, Ubiquitous) or fold it into REQ-CNL-008's bullet body; add a matching AC in §B.6 ([수동] — "대기 목록 각 항목에서 `position`이 `courseTitle`과 함께 표시되고, 전역 순위를 뜻하는 표기가 사용되지 않는다"); add it to §D.2 under INV-FE-004 or as a new INV; add it to `plan.md:353` M6 완료 판정. If it is *not* meant to be normative, strip the `shall`/`shall not` tokens and mark it advisory.

**M3. MANUAL-AC-ABSENT-FROM-DOD-CHECKLIST — `acceptance.md`:L256-270; `plan.md`:L363-377 — The N5 split created a new [수동] AC that was not added to either human-verification checklist. — Severity: minor — Required fix:** `AC-FE-073b` (`acceptance.md:96`) is [수동] ("브라우저에서 페이지를 새로고침하면 … 폴링이 3초 간격으로 재개되고 상한 중단이 최초 접수로부터 30초 시점에 발생한다"), but the §E.2 checklist (`:258-270`) still lists exactly S1-S13 with no refresh-during-polling scenario, and `plan.md` §F M7's scenario table (`:365-377`) is likewise unchanged. §E.2 carries a `[HARD]` marker (`:272`) making it the authoritative human-verification record, and `plan.md:379` M7's completion criterion is "S1~S13 전부 수행". Coverage is not actually lost — `acceptance.md:286` §E.4 requires "§B.1~§B.7 **전부**", and `plan.md:324` M4's 완료 판정 explicitly names "AC-FE-073b 수동" — so this is a checklist-drift defect, not a hole. But it is the same family as round-2 N3 (the §E.2 checklist falling behind newly-added items), recurring one round later on the item the previous fix created. Fix: add `S14 새로고침 후 폴링 재개 → 3초 간격, 상한은 최초 접수 기준` to both `acceptance.md` §E.2 and `plan.md` §F M7, and update the two "S1~S13" range tokens (`acceptance.md:256`, `plan.md:273`, `plan.md:379`, `acceptance.md:286`) accordingly.

**M4. AP-NUMBER-ORDER — `plan.md`:L389-402 — The anti-pattern table's numbering is out of sequence. — Severity: minor — Required fix:** `grep -oE '^\| AP-[0-9]+' plan.md` returns, in document order: `AP-1 AP-2 AP-3 AP-4 AP-5 AP-6 AP-11 AP-12 AP-13 AP-14 AP-7 AP-8 AP-9 AP-10`. Count = 14 with no gaps and no duplicates (so `progress.md:22`'s "안티패턴 **14건**" is accurate), but AP-7 through AP-10 appear *after* AP-14. A reader scanning for AP-7 — which `spec.md` REQ-CNL-004's concern maps to — lands on AP-11 at that row position. Pre-existing since v0.2.0 (the new AP-11~14 were appended mid-table rather than at the end); missed by rounds 1 and 2. Fix: move the AP-7~AP-10 rows above AP-11, or renumber so document order is ascending.

---

## Chain-of-Verification Pass

Second-look findings — what the first pass missed, and how the second pass corrected it:

- **Did I actually read every REQ, or accept the labels?** All 58 read individually in `spec.md`. The first pass confirmed the N1 rationale rewrite and moved on; the second pass asked the narrower question "*what did the fix add?*" rather than "*did the fix remove the false claim?*" — that is how **M2** surfaced. The N1 fix is correct in what it deletes and defective in what it adds, and a diff-oriented pass that only checks the deletion cannot see it.
- **Did I check REQ sequencing end-to-end?** Mechanically, per namespace, with a full ordinal dump and a duplicate check. Output recorded verbatim under MP-1 — not spot-checked.
- **Did I verify traceability for every REQ, or sample?** Every REQ, both directions, via shell loops over the full 58-ID and full AC-REQ-reference sets. The reverse loop surfaced only `REQ-ADX-002`, read in context and re-classified as the same deliberate cross-SPEC annotation identified in rounds 1-2.
- **Did I re-derive N1's underlying fact myself rather than trusting the corrected prose?** Yes — this was the instruction I most nearly under-executed. On the first pass I read `spec.md:104`'s claim that `nextPosition` is course-scoped and found it plausible. On the second pass I read the actual repository file on `main` and confirmed the verbatim JPQL `WHERE w.courseId = :courseId` plus the entity javadoc's `(course_id, position) WHERE status='WAITING'` index. The correction is accurate. Had I skipped this, I would have been trusting round 2's finding *and* v0.2.1's prose without touching source — the exact failure round 1 punished.
- **Did I check whether the N2 fix's SHAs were real and load-bearing, or just present?** Both: `git cat-file -t 871d247` confirms the object exists, `git show 871d247:<controller>` confirms the `mine` endpoints are there, `git ls-tree 29a1560` confirms exactly the three controllers that supply endpoints 1-4/9-12, and the sync-branch commit timeline confirms `871d247` genuinely was HEAD at write time. N2's fix is sound.
- **Did I verify the SPEC's factual claims about the system, or only internal consistency?** This is what produced **M1**, and it came from following the D7 verb rather than from reading the SPEC. The D7 verb reads each referenced SPEC's `status:`; `SPEC-ENROLLMENT-001` returned `completed` where every artifact in the audited SPEC says `draft`/`in-progress`. I then confirmed the merge via `gh pr view 1` and `git log`, confirmed the controller set on current `main`, and compared commit timestamps — establishing that the claim was false *at commit time*, not merely stale. **The lesson from rounds 1 and 2 (verify source, not prose) had to be applied a third time, to a third generation of the same claim.**
- **Did I look for contradictions between documents, not just within?** Yes. M1 is a six-way contradiction between the artifacts and the repository. M3 is a contradiction between `acceptance.md` §B.4 (which has AC-FE-073b) and `acceptance.md` §E.2 + `plan.md` §F M7 (which do not). Neither is visible reading any single artifact in isolation.
- **Did I check Out of Scope for specificity, not just presence?** Yes — five H3 sub-headings at `spec.md:321,328,337,346,354`, each with concrete `-` bullets naming specific excluded artifacts, unchanged and still substantive in v0.2.1.
- **Adjustments made:** Completeness was 0.80 in my draft; downgraded to 0.75 after establishing the 2 m 43 s timestamp gap — a claim that was already false when committed is a different (worse) defect than a claim overtaken by later events, and round 2's precedent scores this class under Completeness. Traceability was 0.92 in my draft; downgraded to 0.90 because M2 places a `shall` outside the REQ namespace, which is a traceability anchor failure even though it does not break the mechanical REQ→AC loop.

---

## STOP Escalation (score regression)

Per the Retry Loop Contract § LEAN Workflow Additions:

```
iter1 = 0.75 (FAIL)  →  iter2 = 0.89 (PASS)  →  iter3 = 0.83 (FAIL)
                                                 ^ regression: −0.06
```

**`STOP` signal emitted.** The orchestrator MUST NOT iterate further unconditionally. It MUST present the user with three options via `AskUserQuestion` (`.claude/rules/moai/core/askuser-protocol.md`):

1. **Reduce scope** (split the SPEC into smaller sub-SPECs)
2. **Accept the current iter-3 verdict with documented debt** (PASS-with-debt)
3. **Explicit user override to continue iterating**

**Honest diagnosis of the regression — read before choosing.** The STOP rule exists to detect *structural* defects that no number of revisions will resolve. **This regression is exogenous, not structural.** The score fell because a dependency merged into `main` at 18:04:24 between the round-2 audit and this one, invalidating a factual narrative the SPEC had correctly documented at round-2 time. The SPEC's structure, requirement set, traceability, and format compliance all held or improved: 8/8 round-2 findings resolved, REQ count and GEARS compliance unchanged at 100 %, traceability mechanically re-verified at 58/58 and 85/85, zero weasel words. Option 1 (scope reduction) is **not** indicated — the SPEC is not too large; a bounded, mechanical edit pass fixes M1-M4. I record this so the STOP signal is not read as evidence the SPEC should be split.

Because this is **iteration 3 of the max-3 cap**, the same three options are also the mandatory post-iter-3 escalation. My recommendation among them, offered as input and not as a verdict: **option 3 (explicit user override for one more targeted pass)** or a scoped equivalent, because M1 is critical, is fully enumerated below, and is cheap to fix — whereas accepting it as debt (option 2) would carry a false run-phase entry gate and a fabricated-override-log hazard into implementation.

---

## Recommendation

**Verdict is FAIL at 0.83** (Tier L threshold 0.85), with one **critical** finding open. No must-pass criterion failed; the FAIL is driven by the aggregate score together with M1's severity.

The v0.2.1 pass itself was competent and honest: all eight round-2 findings were resolved, the two `major` ones were fixed against **source** rather than prose (I re-derived both independently), and the N5 count change was propagated cleanly to all five surfaces that carry the total. Nothing in N1-N8 regressed.

The FAIL is caused by a defect the v0.2.1 pass could not have anticipated but *did* commit into a false state: `DEP-3` was resolved 2 m 43 s before the v0.2.1 commit landed, and the SPEC still tells its reader that run-phase entry is blocked.

Recommended disposition — a **second scoped edit pass**, followed by a **delta-scoped re-audit** (this defect list is the machine-consumable fix route; the re-audit need not repeat the full 58-REQ / 85-AC mechanical sweep, which I have just re-verified clean):

1. **Fix M1 (critical, highest value)** — the 7-part edit enumerated under M1 above. The load-bearing pieces are: `spec.md:116` DEP-3 row → **해소** with evidence `21eab8a` / `mergedAt 2026-08-17T09:04:24Z`; `plan.md` §B.3 collapsed from a live A/B decision to a resolution record (keep §B.3.1's gate-semantics lesson); `AC-FE-907` restated so the `--ignore-deps` branch is **removed**, not merely deprioritized; `spec.md:73-75` rewritten to a single `main`-at-`21eab8a` baseline.
2. **Fix M2 (major)** — promote the `courseTitle`-adjacency / no-global-rank obligation to a numbered requirement (or into REQ-CNL-008's bullet), add one [수동] AC in §B.6, add the INV/§D.2 anchor, and add it to `plan.md:353` M6 완료 판정. Alternatively, if it is advisory, strip the `shall`/`shall not` tokens.
3. **Fix M3 (minor)** — add `S14` (refresh-during-polling) to `acceptance.md` §E.2 and `plan.md` §F M7; update the four `S1~S13` range tokens.
4. **Fix M4 (minor)** — reorder the `plan.md` §G anti-pattern rows so document order is ascending.

**One systemic recommendation, offered because the class has now recurred three rounds running.** Each round has fixed a point-in-time factual claim about `SPEC-ENROLLMENT-001`'s state, and each round the claim has gone false again before the next audit. The `spec.md:73` provenance note was the SPEC's own attempt at a countermeasure and it has now been found false twice in two rounds. Consider replacing the pinned-SHA prose with a **re-verification command** the next reviser runs (e.g. the `depends_on` status grep plus the endpoint-mapping grep), so the artifact carries a *procedure* that survives the dependency moving rather than a *snapshot* that does not. That is a structural fix for the class, not for any single instance.

**Run-phase entry is no longer externally blocked, contrary to what the SPEC says.** `SPEC-ENROLLMENT-001` is `status: completed` on `main`; PR #1 is `MERGED` (`21eab8a`); the `depends_on` pre-flight would now PASS on all three dependencies. **Implementation Kickoff Approval remains mandatory and is unaffected by this verdict** — and per the Phase 1 gate contract, this FAIL blocks Phase 1 progression until the orchestrator resolves it through the STOP-escalation options above.
