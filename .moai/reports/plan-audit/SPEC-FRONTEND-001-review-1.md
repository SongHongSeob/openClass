# SPEC Review Report: SPEC-FRONTEND-001

Iteration: 1/3
Verdict: **FAIL**
Overall Score: **0.75** (harmonic mean of 4 dimensions) — Tier L PASS threshold is **0.85**

> Reasoning context ignored per M1 Context Isolation. The delegation prompt's assertions about backend state (CORS configured, `/mine` endpoints added, SPEC-ENROLLMENT-001 amended to v0.3.0) were treated as hypotheses and re-verified against source, not accepted as fact. Two of those assertions were confirmed; one (SPEC-ENROLLMENT-001 status) was found to differ from the prompt's framing on `main`.

---

## Must-Pass Results

- **[PASS] MP-1 REQ number consistency** — 53 REQ entries across 7 group namespaces, each sequential from `001` with no gaps and no duplicates, consistent 3-digit zero padding. Mechanically enumerated: SES `001-009` (9), CAT `001-006` (6), ENR `001-010` (10), CNL `001-005` (5), ADM `001-010` (10), ERR `001-006` (6), NFR `001-007` (7) = 53, matching the `plan.md:29` claim of "53개 REQ".

- **[PASS] MP-2 GEARS format compliance** — Every REQ carries an explicit pattern label and matches its labelled GEARS form. Samples: `spec.md:144` REQ-SES-001 "(Event-driven) — **When** … **shall**한다"; `spec.md:146` REQ-SES-003 "(Ubiquitous) — 클라이언트는 … 부착 **shall**"; `spec.md:147` REQ-SES-004 unwanted-behaviour canonical negative "영속 **shall not**"; `spec.md:200` REQ-ADM-001 "(Capability gate) — **Where** 세션의 역할이 `ADMIN`이면 … **shall**"; `spec.md:164` REQ-CAT-005 "(State-driven) — **While** … **shall not**". `grep` for legacy `IF … THEN` syntax across `spec.md` returned zero matches — no deprecated-notation exposure.

- **[PASS] MP-3 YAML frontmatter validity** — All 12 canonical fields present with correct types (`spec.md:2-13`): `id: SPEC-FRONTEND-001` (matches `^SPEC-[A-Z][A-Z0-9]+-[0-9]{3}$`), `title` quoted non-empty, `version: "0.1.0"` quoted semver, `status: draft` (valid enum), `created: 2026-08-17` / `updated: 2026-08-17` ISO dates using the **canonical** names (not `created_at`/`updated_at`), `author: manager-spec`, `priority: P1`, `phase: "v1.0.0"`, `module: "frontend"`, `lifecycle: spec-anchored`, `tags:` comma-separated string (not `labels`). Optional `tier: L` (`spec.md:14`) and `depends_on:` (`spec.md:15`) present and correctly formed.

- **[N/A] MP-4 Section 22 language neutrality** — N/A: single-language-scoped SPEC. The deliverable is a TypeScript/React client under `frontend/` (`REQ-NFR-001`, `spec.md:228`); no multi-language tooling surface is covered, so the 16-language enumeration criterion does not apply. Auto-passes.

- **[PASS] MP-5 D7 cross-SPEC reconciliation** — D7 verb executed. Referenced SPEC IDs: `SPEC-AUTH-001` (`status: completed`), `SPEC-COURSE-001` (`status: completed`), `SPEC-ENROLLMENT-001` (`status: draft` on `main`; `status: in-progress` on `sync/SPEC-ENROLLMENT-001`). None carry `retired` / `superseded` / `archived`, therefore **no D7 BLOCKING finding**. (The separate `depends_on` **fulfilment** problem is reported as D5 below — it is a Phase-1 pre-flight gate issue, not a D7 retirement conflict.)

- **[PASS] MP-6 D8 cross-platform discipline** — D8 verb executed: `grep -c 'syscall' *.md` returns `0` for all six artifacts. Per D8-4, absence of `syscall` is auto-PASS.

- **[PASS] MP-7 clarification gate** — `grep -n '\[NEEDS CLARIFICATION' plan.md research.md` returns no matches. (`progress.md:72` mentions the literal token only in a prose statement asserting "0건"; it is not itself an unresolved marker, and `progress.md` is not in the MP-7 subject set.)

**No must-pass criterion failed.** The FAIL verdict is driven by the aggregate score (0.75 < 0.85 Tier L threshold) and by three **critical**-severity factual-accuracy defects (D1, D2, D3) in the artifacts' description of the exact API contract this SPEC exists to consume.

---

## Category Scores (0.0-1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.78 | 0.75 band | Requirements carry explicit rationale blocks (`spec.md:149`, `:172`, `:178`, `:206`, `:209`) and precise negative obligations. Deducted for D4 — `spec.md:174` REQ-ENR-004 mandates a detection rule that `plan.md:122`, `plan.md:317`, and `design.md:49` each explicitly name as the anti-pattern — and D10 (`spec.md:182` permissive "shall할 수 있다"). |
| Completeness | 0.62 | between 0.50 and 0.75 | All required sections present: HISTORY (`spec.md:20-24`), WHY (`spec.md:30`), WHAT (`spec.md:67`), REQUIREMENTS (`spec.md:140`), ACCEPTANCE CRITERIA (`acceptance.md:23`), and 5 × `### Out of Scope — <topic>` H3 sub-headings at `spec.md:259,266,275,284,292` each with concrete `-` bullets. Frontmatter complete. **Deducted heavily** because the consumed-contract surface — the SPEC's own core subject — is materially incomplete/incorrect: 2 endpoints and their DTO contract are absent (D2, D3) and a resolved dependency is described as blocking (D1). |
| Testability | 0.78 | 0.75 band | ACs are binary and carry an explicit verification means column ([자동]/[수동]/[검사], `acceptance.md:13-17`). Zero weasel words found ("적절"/"합리적"/"충분" absent). `acceptance.md:85` AC-FE-063 is an exemplary unknown-value assertion. Deducted for D6 (arithmetic + double-count error in §D.1) and because all 7 ACs in `acceptance.md:121-127` are written against an API contract that does not exist, so they are not executable as written (D2). |
| Traceability | 0.85 | 0.75-1.0 band | **Mechanically verified both directions.** Forward: all 53 REQ IDs appear in `acceptance.md` — zero uncovered. Reverse: every `REQ-*` referenced by an AC exists in `spec.md`, with the single exception of `REQ-ADX-002` at `acceptance.md:108`, which is a *deliberate* cross-SPEC reference to SPEC-ENROLLMENT-001 (annotated "백엔드 `REQ-ADX-002` 연동 확인"), not an orphan. Deducted only for the summary table defect (D6); the underlying per-AC links are sound. |

Harmonic mean: `4 / (1/0.78 + 1/0.62 + 1/0.78 + 1/0.85) = 0.747` → **0.75**.

---

## Defects Found (structured defect-list)

**D1. DEP1-STALE — `spec.md`:L97-101, `plan.md`:L42-54, `research.md`:L24-58, `acceptance.md`:L231,L243, `progress.md`:L68 — Five of six artifacts assert as verified fact that backend CORS is not configured; it is configured on `main` HEAD. — Severity: critical — Required fix:** Re-verify and rewrite the DEP-1 blocks. Evidence of the contradiction: `spec.md:99` states "`common/config/SecurityConfig.java`의 `SecurityFilterChain`에는 `.cors(...)` 설정이 **존재하지 않는다**", and `research.md:35` states "`.cors(...)` 호출이 없고, `CorsConfigurationSource` Bean도 프로젝트 전체에 존재하지 않는다". Actual `main` (commit `29a1560` "fix(security): CORS 설정 추가"): `SecurityConfig.java:48` contains `.cors(cors -> cors.configurationSource(corsConfigurationSource))`, and `SecurityConfig.java:73` declares `@Bean public CorsConfigurationSource corsConfigurationSource(CorsProperties corsProperties)`. The new `CorsProperties.java` javadoc cites "SPEC-FRONTEND-001 research.md §2 DEP-1" — the backend change was made *in response to* this SPEC's research, after the artifacts were authored. Concrete downstream damage: (a) `acceptance.md:243` declares "`DEP-1` 미해소 상태에서는 부분 완료도 불가능하다" — a completion gate keyed on a fact that is no longer true; (b) `plan.md:143` §C.5 and `design.md:200-210` §B.1 mandate a Vite dev proxy as a workaround for a blocker that no longer exists, while `plan.md:320` AP-6 warns against mistaking the proxy for resolution — the whole proxy apparatus is now optional convenience, not necessity, and M1's scope (`plan.md:226`) shrinks accordingly; (c) `acceptance.md:254` F2 ("`DEP-1` 해소 후 프록시 없이 실제 오리진에서 전 시나리오 재확인") is now an in-scope M7 scenario, not a forward-looking check. Rewrite DEP-1 as RESOLVED, record the resolving commit SHA, downgrade the Vite proxy from required workaround to optional dev convenience, and move F2 into `plan.md` §F M7's S-table.

**D2. DEP2-CONTRACT-MISMATCH — `plan.md`:L64, `research.md`:L107-112 — The cancel-target query API is documented at the wrong paths with a wrong DTO field name, and the endpoints now actually exist. — Severity: critical — Required fix:** `plan.md:64` proposes `GET /api/me/enrollments` → `[{enrollmentId, courseId, courseTitle, status, ...}]` and `GET /api/me/waitlist-entries` → `[{entryId, courseId, courseTitle, position}]`; `research.md:108-111` repeats the same. The endpoints implemented on `sync/SPEC-ENROLLMENT-001` are:
  - `EnrollmentController.java` `@GetMapping("/api/enrollments/mine")` → `List<EnrollmentListItemResponse>`, record fields `(Long enrollmentId, Long courseId, String courseTitle, String status, LocalDateTime enrolledAt)` — path differs from the proposal; **DTO matches**.
  - `WaitlistController.java` `@GetMapping("/api/waitlist-entries/mine")` → `List<WaitlistListItemResponse>`, record fields `(Long waitlistEntryId, Long courseId, String courseTitle, Long position, String status)` — path differs, **and field `entryId` is actually named `waitlistEntryId`**, plus an additional `status` field the proposal omits.
  SPEC-ENROLLMENT-001 v0.3.0 `spec.md:216` records the deliberate rationale for the `/mine` suffix over an `/api/me/...` prefix ("경로 접두사를 취소 경로와 일치시킨 이유"). Two further contract properties are absent from every SPEC-FRONTEND-001 artifact: **empty-set behaviour** — REQ-LST-001/002 mandate `200` + empty array, explicitly *not* `404`, so the M6 list screens must not render an empty result as an error; and **ordering** — `enrollmentId` ascending / `position` ascending. Consequence: `plan.md` §F M6 (`plan.md:275-285`), `spec.md` §B.4 REQ-CNL-001~005, `acceptance.md` AC-FE-100~106, and `design.md:157-158` (which shows the call as a placeholder `*(DEP-2 조회 API)*`) are all planned against a contract that does not exist; a run-phase agent following `plan.md:64` literally will call two 404 paths. Fix: mark DEP-2 RESOLVED, replace both paths, correct `entryId` → `waitlistEntryId`, add the `status`/`enrolledAt` fields, and add explicit requirements for the 200-empty-list and ordering contracts.

**D3. CLOSED-WORLD-FALSE — `spec.md`:L69, `design.md`:L185, `design.md`:L148-163 — The endpoint enumeration is stated as a closed world of 12 and is now false. — Severity: critical — Required fix:** `spec.md:69` asserts "이 SPEC이 소비하는 엔드포인트는 아래 12개가 **전부**이며, 이 목록에 없는 엔드포인트는 존재하지 않는다." The backend now exposes 14 (the 12 enumerated, plus the two `/mine` endpoints in D2). The closed-world claim is load-bearing in three places: `design.md:185` fixes the module contract as "`endpoints.ts` # 12개 엔드포인트 호출 함수"; `design.md:148-163` §A.7 maps screens to endpoint numbers 1-12 and leaves the two cancel screens pointing at a placeholder; and `REQ-NFR-005` (`spec.md:232`) requires a single type-declaration point that must therefore cover 14 response shapes, including the two new list-item records. Fix: extend the §A.4 table to 14 rows with the verified `/mine` contracts, update `design.md` §A.1 (backend-record → client-type table), §A.7, and §A.8, and re-state the closure claim against the corrected count.

**D4. TERMINAL-RULE-CONTRADICTION — `spec.md`:L174 vs `plan.md`:L122, `plan.md`:L317, `design.md`:L49, `acceptance.md`:L85 — The normative requirement mandates the exact detection rule its own plan classifies as an anti-pattern. — Severity: major — Required fix:** `spec.md:174` REQ-ENR-004 reads "폴링 응답의 상태가 종단 상태(`SUCCESS`·`WAITLISTED`·`CLOSED`·`REJECTED`·`FAILED`·`CANCELLED`·`PROMOTED`·`NOOP`) **중 하나인 것이 감지되면**" — an enumerated whitelist membership test. Against this: `plan.md:122` "종단 판정은 화이트리스트가 아니라 `PENDING`의 여집합이다"; `plan.md:317` AP-3 "**종단 상태를 화이트리스트로 판정** … 미지 값에서 폴링이 멈추지 않아 **무한 폴링**"; `design.md:49` "종단 목록을 나열해 대조하는 방식(화이트리스트)은 … 채택하지 않는다"; `acceptance.md:85` AC-FE-063 tests the complement rule ("미지의 문자열에 대해서도 종단으로 판정된다"). Since `spec.md` is the requirements SSOT, an implementer following REQ-ENR-004 literally produces precisely AP-3 and violates INV-FE-002. Note this is genuinely future-facing, not academic: I verified the backend domain is currently exactly `RequestState.PENDING` + the 8 `RequestResult` values (`RequestResult.java`, `RequestState.java`, `EnrollmentStatusQueryService.java:51` `String clientStatus = pending ? "PENDING" : request.getResult().name();`), so the whitelist happens to be complete **today** — which is exactly the condition under which this latent defect ships undetected. `plan.md` §C.1 and `design.md:29` both explicitly demand tolerance of future added values. Fix: rewrite REQ-ENR-004 as the complement of `PENDING`, and demote the 8-value list to an informative note supporting REQ-ENR-009's per-result messaging.

**D5. DEPENDS-ON-UNFULFILLED — `spec.md`:L15 (frontmatter) vs `.moai/specs/SPEC-ENROLLMENT-001/spec.md`:L5 — The declared dependency is not `completed`, which hard-blocks the entire `/moai run` invocation, not just M4/M6. — Severity: major — Required fix:** Frontmatter declares `depends_on: [SPEC-AUTH-001, SPEC-COURSE-001, SPEC-ENROLLMENT-001]`. Verified statuses: SPEC-AUTH-001 `completed`, SPEC-COURSE-001 `completed`, SPEC-ENROLLMENT-001 **`draft`** on `main` (and `in-progress` on `sync/SPEC-ENROLLMENT-001`, per its v0.3.0 `completed → in-progress` amendment). Per `spec-workflow.md` § Depends_on Pre-flight Check, "dependency fulfillment is defined as the dependency SPEC's `status: completed` — all other 7 status values … are considered unfulfilled … no partial credit, no score-based bypass." `plan.md:69-75` §B.3 does acknowledge the unmerged PR, but frames it as "시간이 해소한다" and scopes the impact to M4/M6 only, asserting at `plan.md:75` that "M1~M3 … 은 `main`만으로 진행 가능하다". That is true of the *code* but not of the *gate*: the pre-flight runs before the plan-auditor step and blocks the whole run invocation, forcing a wait / `--ignore-deps` override / abort. Fix: add an explicit §B.3 note that the run-phase entry gate blocks at Phase 1 sub-step 0 regardless of milestone, and state which of the three pre-flight options is intended (with rationale) so the orchestrator is not left to improvise at the gate.

**D6. AC-COUNT-ERROR — `acceptance.md`:L163-172, `progress.md`:L22 — The traceability summary table under-counts by one, double-counts 4 ACs across two rows, and omits 4 ACs entirely. — Severity: major — Required fix:** §D.1 totals **76**; mechanical enumeration of `acceptance.md` yields **77** distinct AC IDs. Three separate errors compose: (i) the ERR row claims "6 REQ → 10 AC (AC-FE-001~010)" but that range contains 11 IDs (`005a` and `005b` are distinct); (ii) `AC-FE-001~004` are counted in **both** the ERR row and the NFR row ("7 (AC-FE-001~004, 900·905·906)") — they are NFR-mapped ACs (`REQ-NFR-004`/`003`/`005`), so the ERR row's true membership is `005a,005b,006,007,008,009,010` = 7; (iii) `AC-FE-901`, `902`, `903`, `904` appear in **no** row of §D.1 at all. `progress.md:22` propagates the wrong figure ("인수 기준 76건"). Fix: recompute as SES 16 / CAT 7 / ENR 15 / CNL 7 / ADM 14 / ERR 7 / NFR 7 = 73, plus a 4-row dependency-and-constraint tracking group (901-904) = **77**, and correct `progress.md:22`.

**D7. DANGLING-XREF — `design.md`:L233 — Cross-reference points to a section that does not exist in the citing document. — Severity: minor — Required fix:** §C.2 cites "**§6.3**에서 채택한 서버 상태 도구"; `design.md` has no §6.3 (its sections are §A.1-§A.8, §B.1-§B.3, §C.1-§C.4, §D). The intended target is `research.md` §6.3 (TanStack Query adoption, `research.md:194`). Qualify the reference as `research.md` §6.3.

**D8. CORS-ORIGIN-COUPLING-UNRECORDED — no artifact records it; backend at `src/main/resources/application.properties`:L24 — A new hard coupling between the dev server port and the backend CORS allowlist is undocumented. — Severity: minor — Required fix:** `application.properties:24` sets `app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}`, and `SecurityConfig.java:76-78` restricts allowed headers to `Authorization, Content-Type`, methods to `GET, POST, PATCH, DELETE, OPTIONS`, with `allowCredentials(false)`. No SPEC-FRONTEND-001 artifact records any of this. `REQ-NFR-003` (`spec.md:230`) governs only the *outbound* API base address, not the *inbound* origin the backend will accept. If the Vite dev server binds any port other than 5173 without `CORS_ALLOWED_ORIGINS` being set, every call fails — and per `REQ-ERR-005` the only user-visible signal is the network/blocked classification. Fix: add a `design.md` §B constraint recording the default allowed origin, the 2-header allowlist (relevant because a future custom request header would silently fail preflight), and `allowCredentials=false` (which independently justifies §C.4's rejection of `httpOnly` cookies).

**D9. DEP-TRACKING-AC-STALE — `acceptance.md`:L134-135, L231-243, L253-254 — The dependency-tracking ACs and the partial-completion logic are written under an assumption that no longer holds. — Severity: minor — Required fix:** AC-FE-901 and AC-FE-902 require only that DEP-1/DEP-2 resolution status "be explicitly recorded". Both are now resolved externally, so both ACs pass trivially by recording "resolved" — but §E.4's entire partial-completion decision procedure (`acceptance.md:236-243`) and forward-checks F1/F2 (`acceptance.md:253-254`) were authored under the unresolved assumption and no longer describe the remaining work. Fix: rewrite §E.4 (M6 is no longer conditional, so the partial-completion carve-out should be deleted or re-scoped), and promote F1/F2 from forward-looking checks into in-scope M6/M7 acceptance.

**D10. PERMISSIVE-SHALL — `spec.md`:L182 — A requirement mixes an obligation keyword with permissive phrasing, and its AC tests something stronger. — Severity: minor — Required fix:** REQ-ENR-010 reads "클라이언트는 폴링 실패(네트워크 오류) 시 재시도 **shall**할 수 있다" — "shall be able to retry" is a capability statement, not a binary-testable obligation, whereas `acceptance.md:92` AC-FE-070 tests the stronger "**Then** 재시도한다" (it *does* retry). Fix: restate as an unambiguous obligation ("… 시 재시도 **shall**한다") so the requirement and its AC assert the same thing.

**D11. POLL-BASELINE-UNSPECIFIED — `plan.md`:L113-118, `design.md`:L88-99, `design.md`:L132 — The elapsed-time baseline driving the polling schedule has no defined persistence, while the design makes page reload a first-class entry path. — Severity: minor — Required fix:** The §C.3 / §A.4 schedule is keyed on elapsed time since receipt (1s → 2s → 3s → stop at 30s), but no artifact specifies where that receipt timestamp lives. `design.md:132` defines the route `/requests/:requestId`, which makes reload and direct URL entry first-class paths, and `REQ-ENR-007` (`spec.md:179`) explicitly requires the request identifier to survive. On reload the identifier survives (it is in the URL) but the elapsed baseline does not, so the schedule restarts at 1s and the 30s cap silently resets — a user can hold a stalled request in perpetual fast-polling by refreshing. Fix: state where the receipt timestamp is persisted (alongside the identifier), and add an AC asserting that the cap is measured from original receipt, not from mount.

---

## Chain-of-Verification Pass

Second-look findings — new defects discovered on the second pass, plus confirmation of first-pass coverage:

- **Did I read every REQ or skim after the first few?** Re-read all 53 individually on the second pass. This is how **D10** (`spec.md:182` permissive shall) and **D4** (`spec.md:174` whitelist) were caught — both are mid-document (ENR group) and both were invisible in a first-pass skim that had already accepted the GEARS labelling as uniformly correct. The lesson applied: a correct pattern *label* does not guarantee a correct pattern *body*.
- **Did I check REQ sequencing end-to-end or spot-check?** End-to-end, mechanically, per group namespace — not sampled. Output recorded verbatim under MP-1.
- **Did I verify traceability for every REQ, or sample?** Every REQ, mechanically, in **both** directions via shell loops over the full ID sets. The reverse direction surfaced the single `REQ-ADX-002` reference at `acceptance.md:108`, which I then read in context and classified as a deliberate cross-SPEC annotation rather than an orphan — a sampling approach would have either missed it or misclassified it as a defect.
- **Did I check Out of Scope for specificity, not just presence?** Yes. Five `### Out of Scope — <topic>` H3 sub-headings at `spec.md:259,266,275,284,292`, each carrying concrete `-` bullets (e.g. `spec.md:281` "강좌 검색·필터·정렬 — `GET /api/courses`가 page·size만 받는다" — a specific exclusion with a stated cause, not a vague placeholder). This group genuinely passes; the SPEC's exclusions are among its strongest sections.
- **Did I look for contradictions *between* requirements, not just within?** This was the weakest part of my first pass and the second pass corrected it. Cross-document comparison produced **D4** (spec.md vs plan.md/design.md/acceptance.md on terminal-state detection) and **D6** (acceptance.md §D.1 internal double-count). Neither is visible when reading any single artifact in isolation.
- **New on second pass: did I verify the SPEC's *factual* claims about the system, or only its internal consistency?** Verifying the factual claims is what produced D1, D2, and D3 — the three critical findings. Each is a statement the SPEC presents as directly source-verified ("코드를 직접 확인한 결과", `spec.md:115`; "실제 소스 파일을 읽어 확인한 것", `research.md:3`) and which was true when written but is false now. I read the actual controller and DTO source on the PR branch rather than trusting SPEC-ENROLLMENT-001's prose description of its own M7 endpoints — which is how the `entryId` → `waitlistEntryId` field-name divergence in D2 surfaced.
- **Adjustment made:** Completeness was 0.75 in my draft scoring; downgraded to 0.62 after D2/D3 were confirmed, because the missing surface is the SPEC's own core subject (the consumed API contract) rather than peripheral prose.

---

## Regression Check

Not applicable — iteration 1. `.moai/reports/plan-audit/` contained no prior `SPEC-FRONTEND-001-review-*.md`.

---

## Additional Assessments (requested stress-tests)

### Tier L classification — **justified; not a scope-reduction candidate**

`plan.md:26-34` §A.2 claims 25-35 new files, 53 REQ + 8 INV, 12-endpoint surface, greenfield bootstrap. Per `spec-workflow.md` § SPEC Complexity Tier, Tier L is "> 1000 LOC or constitutional / > 15 files" — the file count alone clears the threshold, and the 5-artifact set was produced as required.

I specifically tested the split hypothesis (member-facing SPEC + admin-console SPEC) and reject it, for a reason internal to this SPEC rather than deference to its own claim: the success criterion is human browser verification of end-to-end scenarios (`REQ-NFR-007`, `spec.md:234`; S1-S9 at `plan.md:296-305`). Scenarios S2 (`WAITLISTED` + 대기 순번), S4 (생성·수정·마감), S5 (정원 증설 → 승격), and S6 (409 축소 거부) **require the admin screens to exist** — there is no other UI path to create a course or fill it to capacity. `design.md:220` confirms the `AdminSeeder` provisions only the admin *account*, not courses. A member-facing-only SPEC therefore could not satisfy its own §E success criteria 2, 4, 5, and 6 without falling back to curl/DB manipulation, which contradicts the SPEC's entire premise. The single-Tier-L decision is sound.

`plan.md:34`'s stated rationale for not downgrading to Tier M ("`design.md`·`research.md`가 생략되는데 … 그 근거를 문서로 남기지 않으면 후속 작업자가 같은 조사를 반복한다") is independently vindicated by this audit: `research.md` §2/§3 is precisely what enabled the backend team to close DEP-1 and DEP-2 — `CorsProperties.java`'s javadoc cites `research.md §2` by name.

### The three tooling decisions

- **TanStack Query for polling — rationale holds.** The five polling obligations enumerated at `research.md:200-204` are real and are genuinely awkward under hand-rolled `useEffect`+`setTimeout`. The `refetchInterval` claim at `plan.md:161` ("함수 형태가 … `false` 반환으로 중단") is technically accurate. The complement-of-`PENDING` framing in `design.md:49` **does** correctly prevent the unknown-future-value infinite-poll bug the delegation asked me to test — I verified the current status domain is exactly `PENDING` + 8 `RequestResult` values, so the design is correct and correctly future-proofed. **However** the framing is undermined by `spec.md:174` REQ-ENR-004 (D4), which is the normative surface an implementer reads first. Two secondary gaps: the elapsed-time baseline persistence (D11), and no artifact specifies the interaction between TanStack Query's own `retry` option and `REQ-ENR-010`'s "retry on network error but never on 401" rule — the 401 exclusion must be wired through `retry`, not only through the §A.3 normalization order.
- **`sessionStorage` for JWT — rationale holds, and is honestly bounded.** The four-candidate comparison at `plan.md:132-137` is grounded in verified backend properties: no denylist, no refresh rotation, 30-minute TTL, and CSRF disabled *because* no cookie credentials are used — I confirmed `SecurityConfig.java:47` `.csrf(AbstractHttpConfigurer::disable)` and the `allowCredentials(false)` at `SecurityConfig.java:78`, which independently strengthens the §C.4 rejection of `httpOnly` cookies (adopting them would indeed force CSRF defence back in). The honest-limitation note at `plan.md:139` ("`sessionStorage`는 XSS 방어가 아니다 … 얻는 것은 지속 기간의 축소뿐") is the kind of statement that should be preserved verbatim through revision — it resists the common overclaim.
- **React Router — rationale holds.** `REQ-ADM-002` (`spec.md:201`) requires direct-address-bar entry to be a testable behaviour, and AC-FE-082 (`acceptance.md:102`) verifies it; conditional rendering cannot satisfy this. Adoption is justified at ladder step 4.

---

## Recommendation

Verdict is **FAIL**. Route the following to manager-spec as a scoped revision. D1-D3 are one coherent work item (the artifacts describe a system state that has since changed) and should be fixed together in a single pass, then re-verified against source rather than against prose.

1. **Re-baseline against current `main` + PR #1 before editing anything.** The artifacts were authored 2026-08-17 ~14:34 and both blocking dependencies were closed afterwards. Re-run the §1 investigation table in `research.md:9-18` against `main` HEAD and `sync/SPEC-ENROLLMENT-001` HEAD, and stamp each finding with the SHA it was verified at, so the next audit can detect staleness mechanically.
2. **Fix D1** — rewrite DEP-1 as RESOLVED across `spec.md:97-101`, `plan.md:42-54`, `research.md:24-58`, `acceptance.md:231/243`, `progress.md:68`. Downgrade the Vite proxy (`plan.md:141-151`, `design.md:200-210`) from required workaround to optional dev convenience, shrink M1's scope accordingly, and move F2 (`acceptance.md:254`) into the M7 scenario table.
3. **Fix D2** — replace `/api/me/enrollments` → `/api/enrollments/mine` and `/api/me/waitlist-entries` → `/api/waitlist-entries/mine` at `plan.md:64` and `research.md:108-111`; correct `entryId` → `waitlistEntryId`; add the `status` field to the waitlist item and `enrolledAt` to the enrollment item; add explicit requirements for the **200 + empty-array** contract (not 404) and the **ascending-order** contract. Unblock M6 (`plan.md:275-285`) — its DEP-2 entry condition is now met.
4. **Fix D3** — extend `spec.md:71-84` §A.4 to 14 rows and update the closure sentence at `spec.md:69`; update `design.md` §A.1 (add both list-item records to the backend-record → client-type table), §A.7 (`design.md:157-158`, replace the `*(DEP-2 조회 API)*` placeholders with endpoints 13/14), and §A.8 (`design.md:185`).
5. **Fix D4** — rewrite `spec.md:174` REQ-ENR-004 to define terminal detection as the complement of `PENDING`, aligning it with `plan.md:122`, `plan.md:317` AP-3, `design.md:49`, and AC-FE-063. This is the highest-value single-line fix in the list: it removes a latent infinite-poll defect that current backend data would hide.
6. **Fix D5** — add to `plan.md` §B.3 that the `depends_on` pre-flight blocks the entire `/moai run` invocation (not only M4/M6), and state the intended resolution path (wait for PR #1 merge, vs `--ignore-deps` with logged rationale).
7. **Fix D6** — recompute `acceptance.md:163-172` §D.1 to 77 with non-overlapping rows and a row covering AC-FE-901~904; correct `progress.md:22`.
8. **Fix D7-D11** — the five minor items, each a localized edit as specified above.

Re-audit scope for iteration 2 is the enumerated D1-D11 delta plus a fresh factual re-verification of §A.4 against source; a full from-scratch re-audit is not required. Note the score-regression STOP clause applies from iteration 2 onward: if the iteration-2 aggregate falls below 0.75, I will emit STOP with a scope-reduction proposal rather than continue iterating.
